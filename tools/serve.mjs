import http from 'node:http';import fs from 'node:fs/promises';import path from 'node:path';import {fileURLToPath} from 'node:url';
const root=await fs.realpath(fileURLToPath(new URL('../',import.meta.url))),port=Number(process.env.PORT||4173),host=process.env.HOST||'127.0.0.1';
const mime={'.html':'text/html; charset=utf-8','.mjs':'text/javascript; charset=utf-8','.js':'text/javascript; charset=utf-8','.json':'application/json; charset=utf-8','.css':'text/css; charset=utf-8','.png':'image/png','.jpg':'image/jpeg','.jpeg':'image/jpeg','.glb':'model/gltf-binary'};
http.createServer(async(req,res)=>{try{let p=decodeURIComponent(new URL(req.url,'http://localhost').pathname);if(p==='/'||p==='/preview/')p='/preview/index.html';if(p.split('/').some(s=>s.startsWith('.'))||p.includes('\\')||p.includes('\0'))throw Error('Forbidden');const file=await fs.realpath(path.join(root,p)),rel=path.relative(root,file),type=mime[path.extname(file)];if(rel.startsWith('..')||path.isAbsolute(rel)||!type)throw Error('Forbidden');res.writeHead(200,{'Content-Type':type,'Cache-Control':'no-store','X-Content-Type-Options':'nosniff'});res.end(await fs.readFile(file));}catch{res.writeHead(404,{'Content-Type':'text/plain; charset=utf-8'});res.end('Not found');}}).listen(port,host,()=>console.log(`Preview: http://${host}:${port}/preview/`));
