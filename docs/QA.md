# 本轮修复验收

2026-09-21 · 运行代码 `c82c209a7636c14781b0bf6597e9ad8a80050ebb` · `D:\星际` · `v3/playable-sc2-demo` · [PR #2](https://github.com/ziyuuu/sc2-survivors-/pull/2)。代码、公开文本摘要和本机原始截图分开保存。

## 规则、构建和离线

- `npm test`：**187 通过，0 失败**。`npm run typecheck`、`npm run build`：退出 0。Vite 保留大 JS 块提示。
- 单文件：**77,303,318 bytes / 73.72 MiB**，89 个内嵌资产；SHA-256 `4126cf04c5462da8ffa47cbd472df5c69127e9129b8100a7674f60e554bda867`。
- 断网 `file://` 启动通过：0 HTTP(S) 请求，8/8 原战斗模型与动作、8/8 死亡模型、3 个经济模型、原降落仓载入；朋友版无 F1 修改状态 API。
- 新覆盖手动架炮反复命令、军衔与科技叠加、晋升保留伤损／冷却／能量、高军衔仍可被围杀、重复建筑与容量预占、双轮跳过／共享刷新、通关奖励一次结算、坡道／崖壁／空地分层和补算积压。
- 两档 2880 秒时钟／关间／虫巢胜负夹具通过；隔离了战斗，不是完整实战胜利。

## 桌面与手机尺寸

桌面以真实键盘事件和正常速度跑满 120 秒，没有修改 HP、资源、兵力或时钟。脚本选择方向：救援 3 个仓、救出 2 个 SCV、击杀 2 个 Drone，普通波次生成 5 只跳虫。进入关间两轮并进入第二关；建筑刷新实际扣 50 矿、类型保留且折扣价格改变。

独立诊断夹具验证毁仓失败、SCV 只结算一次和虫卵期限死亡。夹具不是自然游玩的失败录像。支付／奖励只到账一次由真实规则自动化覆盖。

390×844、844×390：摇杆与推进同时触发、滑出松手、两轮选卡、滚动、无选中文字／误滚动／横向溢出通过。桌面 DPR 1.25 / 2 与离线 DPR 2 原生像素、抗锯齿、画质保存和暂停冻结通过。字体是系统回退；排版通过 DOM 检查，截图未作人工视觉批准。

## 性能

真机 HONOR AMG-AN00 / Android 16 / SM8650 / Adreno 750 / Chrome 148。均衡画质 DPR 1.5，通过 USB 本地地址，无 CPU 人为降速。每场预热 5 秒后记录全部 60 秒 rAF；25／300 为持续补充的高 HP 诊断场景，关闭胆汁、并非真实通关。

| 真机场景 | 实际 CSS 视口 | 平均 FPS | P95 ms | Draw calls | 采样帧 | 模拟推进秒 | 末尾积压秒 |
|---|---|---:|---:|---:|---:|---:|---:|
| portrait-normal-opening | 369×702 | 59.9 | 16.7 | 18 | 3595 | 60.02 | 0.013 |
| portrait-diagnostic-25-300 | 369×702 | 57.2 | 24.9 | 40 | 3434 | 60.07 | 0.004 |
| landscape-normal-opening | 781×252 | 59.8 | 16.7 | 19 | 3591 | 60.02 | 0.012 |
| landscape-diagnostic-25-300 | 780×252 | 57.7 | 24.9 | 41 | 3461 | 60.10 | 0.008 |

最终补测横屏前 USB/CDP 连接中断一次，保留完整竖屏样本并重测横屏；中断部分不计入采样。两种方向均验证摇杆＋架炮的两指输入、松手归零、命令不会重复切换、奖励跳过与页面滚动。结束恢复原常亮／旋转设置并撤销本次端口映射。一台高性能 Android 的分钟级采样不能代表普通低端手机或完整 48 分钟温升。

桌面模拟：AMD Radeon Graphics，DPR 3 / 渲染 1.5，4 倍 CPU 降速，预热 6 秒＋采样 10 秒、统计最近 240 帧。

| 视口 | 平均 FPS | P95 ms | Draw calls | 模拟积压秒 |
|---|---:|---:|---:|---:|
| 390×844 | 24.7 | 50.2 | 39 | 9.95 |
| 844×390 | 26.9 | 50.1 | 39 | 8.78 |

极端降速模拟仍低于目标，且不能实时推进。新增补算预算避免数秒画面冻结；每个固定 tick 保留，过载时进度落后墙钟并明确报告。不能用显示 FPS 冒充完整模拟速度。平地区域查询优化前，真实手机压力场景曾严重卡顿；该失败结果已作废，以上为修复后复测。

## 原素材与动作

- Manifest 89/99 可用；8/8 战斗模型、8/8 死亡模型、2 个坦克形态、3 个经济模型、原仓体，23/23 图标、30 张效果纹理和 8 张地表／崖壁纹理。剩余 10 项为原声音。
- 独立 M3Studio 只读导入：24 个 M3、995 骨骼、208 主模型片段的名称／时长核对无差异。不是 Blender 图像复核。Tank 附加 Flail 只有 126/132 变换 ID 匹配，已拒绝；正常动作来自主模型。
- 浏览器播放覆盖 8 套移动、7 种武器事件（爆虫通过自爆）、8 套死亡、医疗 Stand Work 与实际连续治疗／能量消耗、坦克四个状态，错误 0。原动态枪口挂点与上身混合有网页适配。
- 四种仓体下落、落地、开门、毁仓和动态新增／晋升标签通过；落地子网格不再被失效包围范围裁掉。开门仍为原门骨骼适配。
- 医疗艇两片扰动面排除；实体与透明／发光材质分开、队伍色和原比例修复。Char 真正的高低地、坡道与原崖壁纹理共用模拟地图。
- **视觉人工批准：未完成**。原文件／骨架／运行验证不等于 SC2 客户端像素级一致。截图和状态播放记录位于 `reports/local/qa-v5-assets/`，不上传原图。

## 剩余边界与复现

仍缺 10 个原声音（含 SCV Ready）、Marine 的 fireball_1hot 层和 HellionBeam.m3；完整粒子／折射／动态材质及原字体未复刻。部分死亡包使用已标注的同兵种变体。精确来源及落盘路径见 [缺失清单](ASSET_DOWNLOAD_REQUIRED.md)。

军衔后的理论资源账本见 [平衡报告](BALANCE_12_STAGES.md)。既定敌军与经济预算保持不变；不以自动通关评判普通，未进行完整人工 48 分钟通关。

实际命令：`npm test`、`npm run typecheck`、`npm run build`、`node tools/qa.mjs`、`node tools/qa-effects.mjs`、`node tools/qa-pods.mjs`、`node tools/qa-quality.mjs`、指定 `SC2_ADB_SERIAL` 后 `node tools/qa-phone-v5.mjs`、`python tools/audit-m3-independent.py`、`node --import tsx tools/budget-envelope.mts`。

摘要：`reports/qa/v5-summary.json`、`reports/qa/v5-model-audit.json`、`reports/STATUS.json`；原始日志／截图／剖析均在忽略目录 `reports/local/`。
