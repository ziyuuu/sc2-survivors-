# 当前迭代验收

## 2026-09-25：M2原版四线天赋与局内经济

已按批准的M0 r6稿接入三族165节点、共享80级／80点、分层永久资源、九预设和本局冻结；M2局内经济、微操、付费进阶及玩家爆虫5秒停顿均有针对性回归。`npm run typecheck`、`npm test`（411／411）、`npm run docs:data`、`npm run docs:check`、`npm run build`及开发／离线浏览器存读通过。18组普通开局与第6关参考采样的固定步p95≤1.7ms，但出现最长283ms的个别长帧；不宣称实体设备稳定60+。构建大小、SHA-256、浏览器证据、限制和后续阶段见[项目M2验证记录](project/M2_VALIDATION.md)。

## 2026-09-25：M1三族单一规则与存档验收

M1-01至M1-05完成：新局固定mvp-1.0三族18关、零天赋，读档保留档内种族与难度；旧开发战局只读导出和核验后一次性导入永久本金。精英满编选择可保存、恢复、完成，重复提交不重复结算。旧训练及战斗回退已退役，现行逐乘员订单和无尽独立流程保留。

本次实际执行npm run typecheck、npm test（379/379）、npm run test:save（20/20）、npm run docs:data、npm run docs:check、npm run build、开发版浏览器存读及file://离线存读，均通过。可玩单文件440,929,898字节，SHA-256 C7EBD771A85406BBB3819FAC9F367CE9D1A1885A3F337A7E9A8F0425F1090877。浏览器自动化不等同人工视觉或帧率验收；上一轮普通阶段60+目标仍未完成。完整证据见[项目M1验证记录](project/M1_VALIDATION.md)。

## 2026-09-24：精英救援卡死修复

使用用户导出的旧规则第7关存档复现 `RangeError: Maximum call stack size exceeded`：精英模型尚未登记加载任务就同步通知HUD，HUD再次请求同模型而递归；界面留下旧购买页。领取时中途通知还导致“待领取精英已登记、原掉落未移除”。现先登记共享加载任务再通知，并将拾取效果与掉落移除纳入一次事务通知。精英选择页增加保存、导出与返回标题，保留原选择。

仅为这份已证实异常的导出档生成本地恢复副本：移除已登记精英对应的重复掉落，所有其他字段及两份天赋档案逐字段保持；原导出文件哈希未变。没有为所有旧档猜测性删除掉落，也没有更改天赋运行设计。

新增两项回归在修复前均失败、修复后通过；16项精英专项、全量592项测试、`npm run typecheck`、`npm run docs:check`、`npm run build`通过。真实存档在开发版与正式离线单文件中分别验证：五名可选队员、保存／返回标题／浏览器重载、390×844窄屏键盘选择、保持伤损与冷却、战斗继续、精英不重复晋升。控制台错误为零；离线HTTP请求为零。截图仅本地保存，未进行人工画面或物理设备验收。

浏览器命令：`node --import tsx tools/qa-elite-receipt.mjs`、`node --import tsx tools/qa-elite-receipt.mjs --offline`。当前单文件440,973,335字节，SHA-256 `6855597b49ec3dcf956dcf5f8747d978cae5fbc7a1a5eed1e6772509e8edc17d`；745资源，保留大分块构建提示。[本次证据](../reports/qa/elite-receipt-20260924.json)。本次没有新增帧率结论；天赋原版恢复与独立微操设计仍待确认。

## 2026-09-24：地表渲染优化交付

保留原地图的八层颜色、八层法线、两张混合遮罩与章节揭示；片元只读取混合权重实际大于零的图层。原图遮罩绝大多数像素只用一至三层。载入阶段等待着色器编译，并预上传已开放地图、空投舱、主巢与特效纹理；平面双面特效合并为一次绘制。没有改动60Hz模拟、敌军数量、关卡、产能或画质档。第1关同世界同镜头的本地1440×900画布对照，稀疏与原逐层着色器RGB完全相同；第6关差异像素约0.00231%，低于同样稀疏算法两次截图的0.00247%漂移。这不是人工美术批准。

本机5800U／Radeon、Chrome headless、1440×900、DPR1、普通难度、零天赋。开发版真实选族开局测20秒；第6关使用固定友军高生命的正式波次参考场景。与上一轮相同脚本的旧着色器样本相比，人族、虫族、神族开局由46.3／49.2／46.2升至57.7／59.8／59.9 FPS，第6关人族参考场景由45.6升至58.1 FPS。顺序采样会受主机波动影响，不能把这些差值写成所有设备的固定增幅。

新**正式离线单文件**另测三族自然第一关，每场20秒：人族57.84、虫族58.11、神族58.14 FPS；P95帧间隔均16.8ms，第二个10秒窗口59.72／59.68／60.04 FPS，模拟积债约零，网络请求与控制台错误为零。旧正式包人族样本41.42 FPS。新包前10秒仍有180—233ms的偶发长帧；预热强制离屏绘制未改善，且第6关样本退化，已经撤回。**普通阶段稳定60+尚未通过**，尤其缺正常操作下的第2—6关及实体高刷新设备验收。

独立25普通＋3英雄＋300敌人高压夹具测三次，12秒墙钟推进12.33／12.37／12.18模拟秒，没有新增时间积债；画面约37.7／36.6／33.9 FPS，不能据此宣布高压60帧或三族长期压力达标。590项测试、类型检查、数据文档检查、745资源构建及三族断网续档通过。当前单文件440,972,749字节，SHA-256 `9470d9e6ec194976e68f18ab711b7e7c98aaea87754718e61bfcb09d3888602d`。

命令：`npm run typecheck`、`npm test`、`npm run docs:check`、`npm run build`、`npm run test:offline:save`、`node tools/qa-ordinary-performance.mjs --cases opening-terran-native,opening-zerg-native,opening-protoss-native,stage6-reference --output reports/local/ordinary-performance-20260924-final-dev`、三族各一次`node tools/qa-ordinary-offline-performance.mjs --race <race> --output <local-directory>`、`node tools/qa-performance-probe.mjs --cases 300-native --repeat 2 --output reports/local/performance-probe-20260924-ground-final-repeat --port 5186`。逐项数值与本地报告位置见[公开摘要](../reports/qa/ground-sparse-optimization-20260924.json)。截图只留在本地，不上传。

## 2026-09-24：普通游玩帧率与地表成本探测

本轮**只增加本地探针与证据，不修改游戏玩法、着色器或离线包**。本机 Ryzen 7 5800U／Radeon、Chrome headless、1440×900、DPR1、普通难度、零天赋。开发版以默认关卡种子89241由真实选族／开始按钮进入，随后将随机状态固定为421供对照，保留生产与正式波次；正式离线包完全不使用调试入口。第3／6关是按正式关卡波次运行、固定友军编制并提高友军生命的**参考场景**，不当作实际通关。每场预热2秒，再测15—20秒；只包裹`World.step`与`BattleRenderer.render`，截图保存在本地，未上传或据此宣称人工视觉验收。

| 场景 | 平均交付帧率 | 帧间隔P95 | step均值 | render提交均值 | 模拟积债 |
| --- | ---: | ---: | ---: | ---: | ---: |
| 第一关人族／开发版／清晰 | 46.3 | 33.3ms | 0.38ms | 6.33ms | 约0 |
| 第一关虫族／开发版／清晰 | 49.2 | 33.3ms | 0.47ms | 5.81ms | ＋0.02秒 |
| 第一关神族／开发版／清晰 | 46.2 | 33.4ms | 0.44ms | 6.17ms | 约0 |
| 第一关人族／正式离线包／清晰 | 41.4 | 33.4ms | 未逐步计时 | 未逐帧计时 | 约0 |
| 第三关人族／参考场景 | 43.3 | 33.4ms | 0.55ms | 6.87ms | 约0 |
| 第六关人族／参考场景 | 45.6 | 33.3ms | 0.66ms | 7.45ms | 约0 |
| 第六关／关闭整个渲染的诊断 | 59.9 | 17.0ms | 0.61ms | 约0 | ＋0.02秒 |

同一人族第一关的390×844窄屏约59.8帧，但可见地图网格从桌面约141降至83，绘制批次约159降至98；这是浏览器视口对照，不是实体手机结论。1440×900下切换“省电”仍约45.5帧：DPR1时三个画质档的实际渲染像素比例都是1，省电档并未降低此场景的逐像素地表成本。正式离线包另一场无输入长观察在模拟46.1秒时失败；仍在战斗的前四个10秒窗口分别约33.2、31.0、33.8、40.2帧，因此不能把它当作第二关实战样本，也不能用一次短样本给出稳定帧率保证。两场正式包均无HTTP请求和浏览器错误。

诊断消融只发生在开发版测试页面，**不是游戏改动或可接受的最终画面**：

| 第一关桌面诊断 | 交付帧率 | 说明 |
| --- | ---: | --- |
| 保留全部画面 | 46.3 | 原场景，约159绘制批次 |
| 渲染像素比例0.5 | 57.2 | 视野与批次不变，像素数变为约四分之一 |
| 仅隐藏地表 | 59.9 | 仍绘制约156批装饰，无超过20ms的帧 |
| 仅隐藏地图装饰 | 54.0 | 绘制批次降至约15，仍绘制地表 |
| 仅移除地表法线层采样 | 56.8 | 诊断着色器，不能直接发布 |
| 仅移除地表颜色层采样 | 57.1 | 诊断着色器，不能直接发布 |
| 地表替换为纯色基础材质 | 59.9 | 仍绘制装饰，视觉内容已改变 |
| 隐藏整张地图／关闭GL提交 | 60.0／60.0 | 两者均为定位成本的诊断 |

原地图地表代码在每片元固定读取八层颜色、八层法线，另读两张混合遮罩和一张章节揭示贴图。地表消融与像素缩放都显著改善交付帧率，说明它是本机普通阶段最优先的优化对象；这仍是**消融推断**，尚未测到独立GPU计时。下一步应本地预合成地表颜色／法线或减少每片元采样，保留章节揭示，并对原地图逐像素／人工观感核对。装饰材质与绘制批次随后处理；资源／着色器首次使用的长帧另行记录。不要通过减少模拟、敌军或关卡内容来达标。

当前桌面前关**未达到用户要求的60+帧**。Chrome headless的画面节拍接近60Hz，因此这套自动化最多检验“稳定贴近60”，不能证明物理高刷新屏上的“超过60”。发布验收应在实际设备、正式包和正常操作下覆盖三族第1—6关及关间返回；同时记录P95／P99帧间隔、超过20ms帧比例、模拟积债和画面可读性。原始结果：[自然开局及参考关](../reports/local/ordinary-performance-20260924/results.json)、[地表消融](../reports/local/ordinary-performance-20260924-ablation/results.json)、[着色器消融](../reports/local/ordinary-performance-20260924-shader/results.json)、[正式包短观察](../reports/local/ordinary-performance-20260924-offline/results.json)、[正式包长观察](../reports/local/ordinary-performance-20260924-offline-long/results.json)。


## 2026-09-24：空间查询分层优化

普通单位和敌军的避让查询按地面／空中使用预先建立的空间桶，避开另一层的候选。接触碰撞仍使用原完整空间桶和原候选上限顺序。维京等单位在两次建桶之间改变飞行状态时，查询会暂时回退到完整桶并按当前状态过滤，下一模拟步重建分层桶。桶是可重建临时状态，不入档。

固定种子421、真实地图及原模型、1440×900／DPR1、25普通＋3英雄＋300敌军、原画质的开发版Chrome交错对照；两臂运行同一新包，基线臂只把避让查询切回旧完整桶路径，因此两臂都支付新增建桶成本。每场预热2秒、测量12秒墙钟：

| 对照轮次 | 完整桶：模拟推进／积债 | 空地桶：模拟推进／积债 |
| --- | --- | --- |
| 1 | 8.00秒／＋4.03秒 | 10.00秒／＋2.07秒 |
| 2 | 7.05秒／＋5.05秒 | 9.77秒／＋2.28秒 |

独立密集空地微基准在相同查询数下，桶候选访问14,556降至9,138，查询耗时中位34.79ms降至24.44ms；每帧额外建桶约0.039ms。90步混合空地战斗对照的单位位置、生命、目标、随机状态及碰撞结果一致。两组浏览器结果方向一致，但样本少、状态随推进不同，不能据此推算正式版帧率；300敌时仍有时间积债，实时性能尚未通过。原始对照见 [本轮交错测量](../reports/local/performance-probe-20260924-plane-ab/results.json)。

实际执行：`node --import tsx tools/benchmark-spatial-plane.mjs`、`npm run typecheck`、`npm test`（590通过、0失败）、`npm run docs:check`、`npm run build`、`npm run test:offline:save`。三族单文件断网开局与续档均无HTTP请求或浏览器错误。本轮可玩包为440,971,695字节，SHA256 `e46f880de9cdde21e1161981ceb642b4d76b282b59fbefd049a536b0f3092562`；机器可读收据见 [空间优化记录](../reports/qa/performance-spatial-20260924.json)。


## 2026-09-24：寻路与普通单位LOD优化

`MapTerrain.path`现在为每张地图保留一组寻路数组，用搜索代数标记触及的格子，避免每次A*搜索都分配和清空约10万格的`prev`、`g`、`closed`。邻居顺序、Float64路径费用、堆的并列规则和同步决策时刻不变；阶段切换仍清除路线／可达图缓存，工作数组只作为可重建的临时数据。普通单位远景LOD从旧虫族五家族扩展到三族30家族、死亡模型和已有变形模型；英雄和精英模型不纳入这项简化。

同一台5800U／Radeon、1440×900、DPR1、25普通＋3英雄＋300敌军的开发版压力场景，12秒墙钟结果：

| 场景 | 上轮单样本模拟推进／积债 | 本轮三样本模拟推进／积债 | 本轮三角形均值范围 |
| --- | --- | --- | ---: |
| 原画质 | 7.53秒／＋4.52秒 | 8.58—10.30秒／＋1.72—3.47秒；推进中位9.27秒 | 约89.8—91.4万 |
| 省电档 | 10.37秒／＋1.65秒 | 7.87—11.32秒／＋0.72—4.20秒；推进中位9.57秒 | 约42.4—43.4万；上轮约76.1万 |

三次新样本的敌军存活数均为300，脚本与上轮共用固定种子、初始顺序及12秒计时；每场模拟状态仍会演进，旧数据只有一次采样。省电档三角形数量下降约43—44%，draw仍约219；**不能把三角形减少推断为等比例帧时提升**。原画质的三次结果均优于旧单样本，但波动仍明显，不能宣称已达到300敌实时性。渲染禁用的诊断场景仍接近实时，固定渲染提交及空间查询仍是后续重点。原始数据见 [首轮](../reports/local/performance-probe-20260924/results.json)、[本轮四场](../reports/local/performance-probe-20260924-after/results.json)、[300敌重复](../reports/local/performance-probe-20260924-repeat/results.json)。

本轮实际验证：`npm run typecheck`、`npm test`（587通过、0失败）、`npm run docs:check`、`npm run build`、`npm run test:offline:save`均退出0。新离线包为440,971,153字节、745项无损资源，SHA256 `9be1439f3eb082bea6ade9015d42bb9882a7de4f2bb9e65d009470336dcbff4e`；三族断网开局和续档无HTTP请求及浏览器错误。构建前临时备份已核对后删除。人工英雄与敌军可读性、物理设备及生产版300敌长时GPU表现仍需实测。当前可玩包：[SC2-Survivors-Demo.html](../dist/SC2-Survivors-Demo.html)，机器可读证据见[本轮优化记录](../reports/qa/performance-20260924.json)。

## 2026-09-24：磁盘清理与运行效率探测

本轮不改变玩法和渲染实现。清理按逐文件清单、运行manifest和传递引用执行；删除前核对路径边界、当前试玩包哈希和文件占用。旧V25完整包、V22及更早测试截图／包体、历史性能源码缓存、无引用转换侧车和稳定的LFS临时碎片已清理。旧测试JSON／文字汇总、当前V26证据、Git完成对象／索引／历史、全部运行资源均保留。历史章节提到的早期截图不再保证留在磁盘。

488份仍需使用的原M3／DDS已从Temp按原SHA移回 `assets/private`，临时来源目录已移除。保留292份地图重打包输入GLB。现有一份 `largesmoke1b.dds` 与旧地图来源锁的SHA不同，本轮未修改它或替换现有地图包；这一来源差异不能误报为清理导致的缺失。

清理后实际验证：

- `npm run assets:prepare`：745/745，必需缺失0。
- 三个源生成器 `--check`：普通单位、科技升级和科技球独立来源均通过。
- 资源、动画、原地图、离线封装、源解析相关44项测试全部通过，日志 `reports/local/cleanup-asset-tests.log`。
- `node tools/qa-cleanup-integrity.mjs`：745运行文件及dist逐文件SHA一致，868内部引用有效，488回迁源SHA一致；现有生产版Web三族菜单／部署进入战斗通过，JS／console／HTTP错误与失败请求均0。自建校验HTTP服务前两次曾出现取消，改为完整响应并关闭连接后复测通过，未通过忽略取消来报成功。
- 清理当时的离线HTML未重建，SHA256为 `aa6be4b7a401125376aa93d19a90c47ae1b2adad625869569b13a159a8b004db`；随后性能优化重新构建，当前哈希见上节。清理探测的专用Vite／Chrome及校验服务器均已退出，端口盘点无遗留游戏监听。

新的低开销性能探测：`node tools/qa-performance-probe.mjs`，本机5800U/Radeon，1440×900、DPR1、真实模型和地图，25普通＋3英雄；只包裹step/render，未启用逐方法计时或CPUProfiler。每场暖机2秒＋测12秒，各一场，敌人生命提高以维持数量。它不是自然战役或物理手机结果。

| 场景 | step平均ms | 渲染提交平均ms | 墙钟→模拟秒 | 新增积债 |
| --- | ---: | ---: | --- | ---: |
| 100敌／清晰 | 5.59 | 14.55 | 12.09→12.67 | −0.57秒 |
| 300敌／清晰 | 13.05 | 12.82 | 12.06→7.53 | ＋4.52秒 |
| 300敌／省电 | 10.58 | 11.13 | 12.03→10.37 | ＋1.65秒 |
| 300敌／关闭渲染诊断 | 8.47 | 接近0 | 12.01→12.63 | −0.58秒 |

负积债是消化暖机欠账。关闭渲染仅用于隔离成本，不是玩家优化。四场顺序单样本、状态演进不同，均值差不等于精确优化收益；渲染提交时间也不是GPU完成时间。现有省电档仍未通过300敌实时要求。

下一步优先减渲染固定提交／材质与地图批次成本，补齐新增家族LOD；随后验证空间查询和A*工作区复用，离线解包另测启动峰值内存。保留60Hz、碰撞顺序、敌军数量和积债，不以漏算模拟换取FPS。具体落点与边界见 [只读代码调查](../reports/local/performance-investigation-20260924.md) 和 [受控探测](../reports/local/performance-probe-20260924/README.md)。本轮没有宣称这些优化已经实现。

删除字节／文件数、进程检查和实际命令统一记录于 [本轮维护收据](../reports/qa/maintenance-20260924.json)。删除字节是逐文件累计，不等于C/D盘净空闲变化：期间桌面应用仍处理大型LFS包。40秒监测捕获桌面应用的Git diff包含离线HTML，也捕获LFS过滤进程；15/16旧临时文件前缀匹配当前HTML，但未完整证明过滤器父链或反复中断原因。没有终止应用本身或改写仓库追踪策略。

## V26：三族、局内构筑、50点天赋与18关（2026-09-24）

**代码与可玩包已落地；586项测试通过，0失败／0跳过。当前是可玩验证版，尚未达到用户约定的正式完成标准。** 类型检查、数据文档生成／一致性、生产构建和开发浏览器保存回归均实际执行通过。

本轮实现：

- 三族开局、30普通家族、15英雄候选、40精英定义；每局普通五家族×五身体、英雄三个身份，模式与截击机不重复占席。新增单位、英雄和精英均接入本地原模型。745/745资源准备完成，61个新增模型及动画在本地Chrome加载检查零错误；人工比例、动作与可读性验收仍未完成。
- 双产出持续生产、逐乘员付款、关闭产线不取消付款批次、安全出舱时换组、5级继承到3级、旧订单按状态退款／弃置、完整决策暂停与重复提交保护。建筑付费三选一、免费强化三选一、刷新／保底／上限、地图强化预算、生命修复服务接入同一模拟层。
- 117天赋节点、九预设；每级固定3资源／占1点，最多50点，终极也只占1点。旧投资按冻结的旧价格恢复本金；旧12关、35普通席、原商店及冻结天赋分支继续运行。
- 原生护盾／护障、全效与1/3医修、共享施法预约、地形能力、手动模式／闪烁、隐形与G侦测、付费截击机、英雄槽和可读标记。源武器与科技增量逐段处理；雷神导弹目前保留本引擎即时命中适配，源发射时刻有记录，但不宣称与原客户端弹道时序完全相同。
- 18关1800秒、固定4684原始威胁预算、六章经济、6／9／12关英雄机会、主巢三阶段和双重胜利条件、保留状态进入无尽。

实际验证与证据：

| 项目 | 结果与范围 |
| --- | --- |
| 规则／迁移／战斗／构筑 | `npm test` 586/586；日志 `reports/local/v26-tests-final.log` |
| 类型／数据文档 | `npm run typecheck`、`npm run docs:data`、`npm run docs:check`通过 |
| 桌面／窄屏操作 | `node tools/qa-three-race.mjs`，1440×900与390×844、三族选择／生产／换兵／选卡／侦测／英雄／闪烁取消，零浏览器错误；触屏仿真与模拟手柄，不是物理设备 |
| 主巢表现 | `node tools/qa-main-hive.mjs`，两尺寸扇形／区域／直线／弹道均存在，零错误；截图只保存在本地 |
| 开发浏览器保存 | `npm run test:browser:save`通过7类流程：原模型交替攻击、受伤与冷却、空投、永久档案、相同关间选项、无尽地图、失败档、损坏导入保护、窄屏菜单 |
| 离线三族保存 | `npm run test:offline:save`通过；实际生产单文件、断网file://、三族分别新开与恢复，种族／家族／冻结天赋／钱包／时间一致，HTTP(S)请求0、浏览器错误0，无可修改状态的Debug API |
| 九Build经济 | 9条×3种子共27组公开交易与真实付款／生产通过；零天赋、无战损且假设成功救援，**不是27场实战通关** |
| 零天赋战斗抽样 | 两版控制器在原地图／平地尝试三族，第二版在第1—4关阵亡，没有证明首六关可持续通关；脚本没有崩溃／NaN不等于平衡通过 |
| 300敌军性能 | 六组真实AMD Chrome采样零错误，但桌面12秒墙钟仅推进5.98／8.28／6.17秒模拟，**仍有积债，未通过**；开发构建含CPU采样开销，不代替生产构建或实体手机测量 |
| 原地图优化 | walkLine 43,200随机路径＋8,192原地图路径与旧判定一致；高度调用减少45.54%，微基准查询中位耗时减少4.50%，未据此宣称整体帧率已通过 |

V26当时的[离线Demo](../dist/SC2-Survivors-Demo.html)：**440,970,449 bytes／420.54 MiB**，745项无损内嵌资源；SHA-256 `aa6be4b7a401125376aa93d19a90c47ae1b2adad625869569b13a159a8b004db`。此数值为历史构建，当前包已由上节性能优化版本替换。Vite大JS块提示保留。构建时曾完整备份旧包；该旧包已在授权清理中删除。成果仅在本地工作区，未提交或推送。

尚未通过：三族完整18关真实操作／持续生存与平衡、零／中／50点和各难度抽样、300敌军实时性能、人工英雄可读性和原动作对照、实体触屏／手柄与长期内存表现。原Acropolis装饰及悬崖模型缺口沿用历史记录。不能把数据齐备、单元测试、模型加载或经济推演计作这些项目完成。

详细记录：[实施状态](ROADMAP.md)、[当前机器可读状态](../reports/STATUS.json)、[V26交付证据](../reports/qa/v26-runtime.json)、[性能记录](../reports/local/three-race-performance.md)。

---

以下V25及更早记录是对应版本的历史证据，不代表本轮功能范围。

## V25：英雄参战识别设计（2026-09-24）

**401项测试通过，0失败／0跳过；类型检查、数据文档检查和单文件构建通过。** 本轮只改变战场呈现，不改英雄技能、伤害、目标选择、编制或存档格式。

- 三名英雄同时参战时，使用现有原头像做头顶徽标，持续显示姓名、等级、真实生命、技能就绪／冷却。雷诺为蓝色、泰凯斯为橙色、诺娃为紫色的地面信标；原V24友军精英维持较小的紫色次级标记。施放技能后1.2秒内显示“英雄名 · 技能名”。标签避让且不接收战场输入。
- Chrome headless在1440×900实测三名英雄的原头像加载、三个实例化信标、技能状态和界内位置；手动设置一次诺娃已有施放时间，检查施放短提示响应。390×844仍有三张英雄徽标，宽度无横向溢出、标签均在屏内。浏览器无模型／页面错误。截图仅保存在 `reports/local/qa-hero-readability/`，没有上传。
- 重跑上一轮浏览器流程：诺娃原武器动作／枪口、英雄／精英材质层级、兵营2种／重工厂2种／星港1种生产选择、存档刷新及390×844关间布局均通过。
- 新单文件 `file://` 断网保存／继续通过，HTTP(S)请求0、浏览器错误0、无修改状态的Debug API。自动浏览器检查证明元素和布局存在，**尚不能证明实际玩家觉得足够醒目**；需要试玩观感反馈。

最终 [离线Demo](../dist/SC2-Survivors-Demo.html)：**227,823,090 bytes／217.27 MiB**，无损打包652项资源；SHA-256 `497248cebfe45ada126597f95c95149053b4723261d286656a70cf1e86b4d5b9`。实际执行 `npm run typecheck`、`node tools/qa-hero-readability.mjs`、`npm test`、`npm run build`、`node tools/qa-v23.mjs --offline`、`npm run docs:check`、`node tools/qa-v24.mjs`，并运行 `git ... diff --check`（排除已追踪的HTML和本地LFS过滤器）。[文字证据](../reports/qa/v25-runtime.json)及[当前状态](../reports/STATUS.json)记录本轮结果。成果仍在本地工作区，未提交或推送。

---

以下V24及更早记录是对应版本的历史证据。

## V24：本轮生产、诺娃装配与特殊单位辨识（2026-09-24）

**401项测试通过，0失败／0跳过；类型检查、数据文档一致性检查与离线构建通过。** 当前仍为12关、原五种普通友军及既有容量；18关、新兵种、科技建筑链和有损换组尚未实装。

- 关间建筑界面显示“本轮生产”。兵营、重工厂、星港分别可在当前已解锁配方中选一至两种；选两种时仅两者交替。规则测试确认变更只作用于之后开工的批次，不改写已付款共享Job。V23续局载入后补默认生产选择，完整档案继续保留。
- 诺娃步枪贴图和导出PNG逐像素核对无误；枪械离手的根因是原GLB中 `Bone_Gun` 的局部装配偏移。运行时把该骨骼回到手部参考位置，仅显示当前A动作使用的步枪网格；没有重写原GLB或贴图。浏览器载入真实模型，检查A动作、三个有效网格与修正后的枪口坐标。
- 英雄／友军精英分别有更明显的金／紫色边缘光、双层脉动环、竖向标记、开火闪光和名称样式。Chrome headless实际显示诺娃与突击枪兵文字标签，渲染器无模型错误；这些视觉事件不改伤害。尚未做原客户端画面人工对照。
- 开发浏览器在1440×900检查兵营2种、重工厂2种、星港1种；点击取消一种后键盘焦点保持、刷新继续仍保存选择。390×844宽度无横向溢出。截图只写入本机 `reports/local/qa-v24/`，未上传。
- 最新单文件 `file://` 断网保存／继续通过，HTTP(S)请求0、浏览器错误0、无修改状态的Debug API。该离线冒烟测试验证包与存档；生产交互是在开发浏览器中验证。

最终 [离线Demo](../dist/SC2-Survivors-Demo.html)：**227,819,145 bytes／217.27 MiB**，无损打包652项资源；SHA-256 `e2763685d8575fd258aacfffd263674b807fca3299d86a4fe6ba31b6ea600182`。大JS块提示保留，本轮没有性能提升测量。

实际执行：`node --import tsx --test test/batch-production-v15.test.ts test/save-v23.test.ts`、`npm test`、`npm run typecheck`、`npm run docs:check`、`npm run build`、`node tools/qa-v24.mjs`、`node tools/qa-v23.mjs --offline`、`git ... diff --check`（绕过本地LFS过滤器并排除已追踪的HTML）。[文字证据](../reports/qa/v24-runtime.json)与[当前状态](../reports/STATUS.json)记录本轮结果。没有真人整局、实体手机／手柄或原客户端人工视觉验收。当前成果仍在本地工作区，未提交或推送。

---

以下V23及更早记录是对应版本的历史证据，不代表本轮重新运行了全部旧版检查。

## V23：劫掠者动作、战局存档与结构文档（2026-09-24）

**399项测试通过，0失败／0跳过；类型检查、数据文档一致性检查、构建通过。** 死神／跳跃未接入，其他新兵种仍是候选。普通编制25／已有天赋35、英雄另3席保持，未来新类型由玩家在原容量内选择。

- 原始Marauder M3字节未修改，散列与锁定CASC一致。真实浏览器使用原GLB验证首发右手、第二发左手、Ready和Walk清除；GPU动作帧区间分别从53与0开始，左右枪口采样相距约0.8493世界单位。GLTFLoader规范化后的挂点名称已正确绑定；三个精英劫掠者的原动作／节点名也独立读取核对。
- 新增明确的战局DTO与原子IndexedDB完整档案，主档加两份备份；保留Map／Set／无限计时值、共享生产Job和飞弹命中集。自动保存按10秒战斗时间及交易／暂停／阶段边界；恢复原伤损、冷却、订单、商店、随机状态与冻结配点。旧天赋pending及缺主档备份恢复通过。
- Chrome headless 1440×900实测：战斗刷新继续保持HP／冷却／钱包／舱乘员／状态／永久档；关间卡牌／收据不重抽；Acropolis及五座设施恢复；败局不出现继续；完整文件导入有效、损坏导入保留原档。390×844界面宽度检查无横向溢出，继续按钮初始可获焦点。截图只留在本地，不作为人工视觉认可。
- 最终单文件断网 `file://`：保存后刷新继续成功，模拟时刻与统计相同，HTTP(S)请求0、浏览器错误0、无修改状态Debug API。
- 入口生命周期、存档协调／后端、纯天赋／进化模板与出生缓存已分离。文档清除旧V18现状冲突，并新增从源码生成的10单位、四难度12关、英雄／精英与55天赋节点参考；`docs:check`通过。

最终 [离线Demo](../dist/SC2-Survivors-Demo.html)：**227,813,401 bytes／217.26 MiB**，全部652项资源保留；SHA-256 `3029d48e99bf70bef9aa6cc8ccecbb01bbfe29f07e19fff93af4fae66ff55392`。构建保留大JS块提示，没有据结构调整宣称FPS提升。

实际执行：`node --import tsx --test test/save-v23.test.ts test/animations.test.ts`、`npm test`、`npm run typecheck`、`npm run docs:data`、`npm run docs:check`、`npm run build`、`node tools/qa-v23.mjs --development`、`node tools/qa-v23.mjs --offline`。另做原M3哈希／GLB节点／本地ActorData读取与核心文档链接检查。初次浏览器枪口检查失败暴露名称规范化遗漏，修复后完整复跑通过；失败没有计为通过。

[公开文字摘要](../reports/qa/v23-runtime.json)、[当前STATUS](../reports/STATUS.json)。本地日志：`reports/local/v23-tests.log`、`v23-build.log`、`v23-browser.log`、`v23-offline.log`；截图／导出测试档在 `reports/local/qa-v23/`。

本轮没有人工原客户端逐帧对照、真人完整战役／长期平衡、手机真机、实体手柄或性能提升验收。Acropolis缺失装饰／崖壁范围仍沿用V22记录。代码与构建是本地工作区成果，本轮未提交或推送。

---

以下是历史版本证据，不作为本轮新增验收。

## V22：困难／地狱、永久天赋与 Acropolis 无尽战场（2026-09-23）

困难和地狱现在可选。敌军按关卡、难度及精英等级成长；四位领主带专属攻击，蟑螂腐蚀护甲与毒爆虫持续胆汁等状态有可见剩余时间。四线七层永久天赋以资源点购买，带本地备份、导入导出与洗点。第 12 关后进入独立的 Acropolis LE：小队 ID、HP、冷却与资源延续，中央附近布置四座自动地堡和一座修理设施，旧战场敌军与未结事件清除。

Acropolis 原始 SC2Map 的高度、地表遮罩和通行数据已转换；**51,023** 个开阔连通采样点、16 个已有模型种类与 150 个原摆放可用。原地图指定的前八层地表 DDS 从固定的 SC2 5.0.16.97563 CASC 版本本地提取，保留 21 张来源散列锁，再合成漫反射／法线图集。仍缺 **3,413 个装饰摆放**及专用崖壁模型，所以目前不宣称 Acropolis 完整原貌或原客户端逐像素一致。

`npm test`：**387 通过，0 失败**；`npm run build` 包含类型检查，通过并准备 **652/652** 项资源。单文件 [离线 Demo](../dist/SC2-Survivors-Demo.html) **227,791,278 bytes／217.24 MiB**，SHA-256 `5a3dd6450b9f1c5fdcf1849a5feca47b7ef5a345d0f6ad69008321443c50f269`。本地 Chrome 自动化验证了天赋购买、切图后五座设施与存活小队、390×844 天赋页面布局，以及断网 `file://` 启动；五项 Acropolis 地图／贴图／遮罩内嵌资源逐字节散列一致，HTTP(S) 请求 0、渲染错误 0。[本机浏览器记录](../reports/local/qa-v22/report.json)。同一短时 headless 样本为 208 draw calls、270,474 triangles、约 38.7 FPS；不代表真机或长局性能。

实际命令：`pwsh -NoProfile -File tools/discover-acropolis-casc.ps1`、`pwsh -NoProfile -File tools/fetch-acropolis-casc.ps1`、`npm run assets:acropolis`、`npm test`、`npm run build`、`node tools/qa-v22.mjs`。本轮没有真人完整通关、原客户端画面对照、手机真机或实体手柄验收；截图仅留在 `reports/local/qa-v22/`。

## V20：普通后期 Boss 伤害（2026-09-23）

普通难度第 6、9、12 关 Boss 的普通攻击和特殊技能伤害提高 20%，普攻攻速分别提高 5%、10%、20%；第 3 关及简单难度伤害和攻速不变。Boss HP 倍率保持普通 90%、简单 50%，护甲、刷新时间和阶段存活规则不变。新增规则测试覆盖四个 Boss、两种难度、伤害/攻速倍率与 HP。

## V19：Boss 生命值按难度缩放（2026-09-23）

四个 Boss 出生时统一使用普通 90%、简单 50% 的配置基准生命值；护甲、伤害与刷新时间不变。生成后及跨关仅保留当前 HP，不再次应用倍率。新增测试覆盖两档全部四个 Boss 和固定其他属性。全量测试 367 项通过，类型检查与构建通过；离线 Demo 为 206,697,451 bytes，包含 647 项素材。

## V18：移动优先、自动走射与输入设置（2026-09-22）

运行提交 `3684549dda4b2a636d5a19e9e74cfea47ece1146`；**365 项测试、类型检查、正式构建通过**。PR #2 的目标分支仍为 `v2/sc2-assets-ui`。旧集火追击断言已按新确认的操作规则替换；地图、生产、难度、经济和原素材保持。

- 自动索敌不再生成追击指令。鼠标左右键／触屏轻点只保存目的地；松开键盘／摇杆不追逐旧目标。复现「另一侧先刷怪→朝空投移动→守军稍后出现」的测试通过，方向不反转，前方守军在一次 0.24–0.285 秒判断间隔内替换旧目标。
- 人族移动中实际停步瞄准／出弹，之后留出至少 0.22 秒移动；移动前摇至少 0.10 秒，持续瞄准超过 0.38 秒后重新尝试移动。实际出弹时间校验阻止移动／站定转换缩短武器周期，保留手动架炮与医疗治疗。参数是手感初值，不声称测量过普通 SC2 玩家的 APM；新增转向参数仅作用于人族，虫族保留原转速并有专门回归覆盖。
- 前排在冷却间继续展开；后排可绕开站定射击者，狭窄地形仍用原行军导航。四兵种混编和十五名射手均能进入射程并持续开火；原地图 25 人通过全部 **32/32** 双向坡道，无越界。[通行记录](../reports/qa/v18-navigation.json)。
- 真实浏览器事件检查 **1440×900、390×844、844×390**：电脑鼠标／键盘、手机点击／摇杆独立切换；未选方式不误下令；设置冻结战斗，重开保留偏好。多指移动＋技能、滑出捕获／取消释放、拖动不误点击、无文档滚动或文字选择通过。手柄 API 模拟检查六个固定右杆方向、A／X 施放、左杆同时移动、松杆不追敌、菜单不放技能及断线暂停。[输入记录](../reports/qa/v18-controls.json)。
- 开发／离线各连续重开五次，World、模型与资产缓存复用，新增模型请求 0。离线首开 **24.31 秒**，同页重开 **17.3–28.9 ms**。断网 `file://` 启动／部署成功，HTTP(S) 请求 0，朋友版无修改状态 Debug。[重开及离线记录](../reports/qa/v18-runtime.json)。
- HTML **206,697,492 bytes／197.12 MiB**，SHA-256 `ab6861f3de34bd89f9b8ff962c2d3430990c1a2d177feff0f4373a04e62a1795`。全部 **647** 项原素材还原字节一致，**31** 个图标解码通过；性能场景载入 **28/28** 战斗模型与 **10** 个原声音。逐动作播放沿用 V15 历史证据，本轮没有新的逐动作人工视觉批准。[包体记录](../reports/qa/v18-pack.json)。
- 单独满载诊断：Windows Chrome headless、AMD Radeon/ANGLE D3D11、1440×900、DPR1、均衡、28 友军／300 敌军，15 秒预热／12 秒采样。平均 **39.61 FPS**，P95 **33.4 ms**，**273 draw calls**；模拟步平均 **7.12 ms**，渲染平均 **11.76 ms**，结束积压 **0.0001 秒**。这不是严格同条件前后提升证明、手机性能或自动通关标准。[性能记录](../reports/qa/v18-performance.json)。

本轮截图只保存在本机 `reports/local/qa-v18/`，没有上传。未测试手机真机或实体手柄，也没有新增真人完整通关验收。旧非标准手柄可能需要补充一次右杆校准；首次载入仍约 24 秒，字体仍使用系统回退，Vite 大 chunk 警告不影响构建。困难／地狱／局外天赋仍未实装。

实际命令：

```powershell
npm test
npm run typecheck
npm run build
npm run test:browser
$env:SC2_CROWD_OUT='reports/local/qa-v18/navigation.json'
node --import tsx tools/audit-map-crowding.mts
$env:SC2_QA_OUT='reports/local/qa-v18/restart-offline'
node tools/qa-v17.mjs
$env:SC2_PERF_EXPANSION='only'
$env:SC2_PERF_OUT='reports/local/qa-v18/performance'
node tools/qa-runtime-performance.mjs
```

`npm run test:browser` 现在指向当前 V18 控制脚本，运行前保持本地开发服务器开启。

## V17：运行重构、普通微调与后续设计（2026-09-22）

运行提交 `3e767d900b8b2a7da4b740172033cadf031023b2`，**361 项测试／类型检查／正式构建通过**。PR #2 仍指向 `v2/sc2-assets-ui`。[当前状态](../reports/STATUS.json)。

- 普通波次／守军按旧锁定预算的 90% 累计取整，首仓仍两只跳虫；简单仍为旧预算的 50%。经济、敌方属性及 Boss 参数未调整。十项新增测试覆盖难度、重开状态、劫掠者展开、遮挡复用、通行快速检查与新资源编码。
- `RunState` 单独管理每局状态；World、控制器、音频上下文、原地图及动画 GPU 资源复用。清理过期仓体及其独立骨骼纹理，防止长局与重开累积。稳定家族席位显示“队伍1”，投仓用独立“增援1”序号。
- 开发／离线各连续重开五次，恢复单枪兵、兵营、50/0、首关与空成长；难度／画质保留。开发每次先创建原模型仓体，重开后 WebGL 对象计数稳定在 **356 geometry／315 texture／87 programs**，监听器数不增长，原 World 与模型实例相同，旧名称／粒子／订单／英雄消失。该计数不等同于显存字节测量。
- 离线首次可部署 **22.92 秒**，五次重新部署 **14.3–23.2 ms**（点击至菜单就绪绘制帧）。只证明同页重开，关闭浏览器后仍需首次载入。[重开与离线记录](../reports/qa/v17-runtime.json)。
- 包体 **206,691,735 bytes／197.12 MiB**，较 V16 的 212.09 MiB 减少 **7.06%**。SHA-256 `8fbb3594d2951fdffb0df35ff6c31f0a71702935e774afc7268fe1d2628613d3`。Base85 减少内嵌编码膨胀，运行清单只保留必要字段，完整来源／材质报告仍保存在本机。全部 **647** 项还原字节与原文件完全相同，**31** 个原图标解码通过；未修改或移除原模型、地图、纹理、动作、声音。[包体统计](../reports/qa/v17-pack.json)。
- 同素材／同内嵌解码器的两个离线诊断页：Windows Chrome、AMD Radeon 集显（ANGLE D3D11）、1440×900、DPR 1、均衡画质、28 友军／300 敌军，预热 15 秒／采样 12 秒。平均 FPS **33.87 → 35.65**（约 **5.3%**）；P95 帧耗时 **49.8 → 33.5 ms**；平均模拟步 **7.82 → 7.39 ms**，渲染 **13.27 → 11.79 ms**。Draw calls **266 → 272**，本轮收益来自 CPU 检查／布局与矩阵工作，不宣称合批减少了调用数。预热结束时积压不同，原始起止值均保留；这是诊断样本，不是同一帧战斗回放、真人通关或稳定帧率保证。[性能证据](../reports/qa/v17-performance.json)。
- 性能场景加载 **28/28** 战斗模型（10 基础＋15 精英＋3 英雄）、10 个原声音。逐动作播放历史证据保留在 V15；本轮验证原字节未变，不把 28/28 加载称为新的逐动作视觉批准。
- 原地图 **32/32** 坡口双向 25 人移动通过，0 卡住／越界。通行快速检查还与原逐点方法随机对照 20,000 次，含扩展后缩回首关。[通行记录](../reports/qa/v17-navigation.json)。
- 1440×900、390×844、844×390 的紫卡拥有状态、精英名称、英雄 HP、无尽入口及下一轮检查通过。两种手机尺寸的模拟多指移动＋技能、取消、手柄英雄键与菜单隔离通过。断网 file:// 部署／右键移动通过，网络请求 0，朋友版修改状态 Debug 不存在。截图全部只留本机；未进行手机真机、实体手柄或新增真人三十分钟通关。[画面结构／无尽回归](../reports/qa/v17-regression.json)／[输入回归](../reports/qa/v17-controls.json)。
- [困难／地狱／天赋树](DIFFICULTY_AND_TALENTS_PROPOSAL.md) 仍为设计稿；四线天赋树按最新资源点规则更新在 [TALENT_TREE_V20.md](TALENT_TREE_V20.md)，尚未实装。当前游戏只有简单／普通，也没有局外资源点或天赋效果。原字体仍用系统回退，首次准备成本仍较大；Vite 的大 JS chunk 提示保留，构建成功。

实际命令（所有脚本与素材处理均在本机）：

```powershell
npm test
npm run typecheck
npm run build
node tools/qa-v17.mjs
$env:SC2_QA_OUT='reports/local/qa-v17-regression'
node tools/qa-v16.mjs
node tools/qa-controls-v15.mjs
$env:SC2_CROWD_OUT='reports/local/v17-navigation.json'
node --import tsx tools/audit-map-crowding.mts
$env:SC2_FIXTURE_OUT='reports/local/qa-v17-file-fixtures'
node tools/build-performance-fixtures.mjs 521a87a
# SC2_PERF_EXPANSION=only，SC2_PERF_URL 分别为两个本地诊断 HTML
node tools/qa-runtime-performance.mjs
```

## V16：无损离线包、友军辨识与无尽（2026-09-22）

运行代码 `179f00c32dedf5fe2223b73cea26beacf3f7ca89`；分支与 PR #2 的目标保持不变。**351 项测试、类型检查、正式构建通过。** [当前状态](../reports/STATUS.json) / [浏览器记录](../reports/qa/v16-runtime.json)。

- HTML 从 **372,231,822 bytes / 354.99 MiB** 降到 **222,391,244 bytes / 212.09 MiB**，减少 **40.25%**。内容哈希去重掉 56,371,905 字节重复分片，唯一资源 gzip 后 164,334,996 字节，再 Base64 内嵌。全部 647 项原文件在浏览器还原后逐项 SHA-256 相同，未删除、重采样任何原纹理、动作、地图。详细 [包体统计](../reports/qa/v16-pack.json)。
- 正式文件 SHA-256：`ecd120aa7f27649e0a70564aa6c8cf3a0bd5c9b9f1c381e9e103a564908732b0`。断网 `file://` 启动、部署、右键移动通过，HTTP(S) 请求 0，修改状态 Debug 不存在。本次本机冷页面至可部署约 **23.61 秒**，仅一个本机样本，不作为通用启动时长或内存节省承诺。
- 紫色精英战场姓名/军衔、卡牌未拥有/已拥有/待编入、同卡晋升后即时更新均通过。三位英雄的金色名牌、模型高度投影、互不重叠的独立血条、HUD 数值、低血量/阵亡状态已检查；技能无合法目标时仍保持血条明亮。头像与模型保持原资产。
- 1440×900、390×844、844×390 检查可见名称、HP 值、血条宽度、HUD 范围、无页面横向溢出、无浏览器/着色错误。沿用触屏/手柄脚本复测 390×844、844×390 的移动+技能多指操作、取消、方向键英雄技能/菜单隔离。仅尺寸/输入模拟，未做手机真机或实物手柄。
- 桌面点击第十二关胜利后的无尽入口，验证原地图源哈希/面积/模型数、原单位身份/生命及虫巢身份保持；生成小兵、强化精英、Boss，连续建筑/随机选卡后进入无尽第 2 轮。规则测试覆盖解锁限制、30/60/90 秒边界、不同怪物轮换不回落、技能伤害一次缩放、原预警延迟、暂停、重复结算及失败。
- 28 友军/300 敌军场景实际载入 10 基础 + 15 精英 + 3 英雄，渲染错误 0。原动作逐款播放的详细证据保留在 V15；本轮所有动作文件已证明字节未变，不把该事实称为新增人工视觉批准。

### 本轮性能与限制

桌面 Chrome headless / AMD Radeon ANGLE D3D11、1440×900 DPR1、均衡、预热 15 秒、采样 12 秒。28 友军（15 精英与 3 英雄）/300 敌军，平均 **36.90 FPS**、P95 **33.5 ms**、**273 draw calls**、每模拟步平均 **6.59 ms**、每渲染调用 **12.16 ms**、末尾积压 **0.0166 秒**。[原始记录](../reports/qa/v16-performance.json)。采样后仅修改了无尽特殊敌人增长；该普通诊断场景未运行无尽分支。

此样本不构成前后同条件 FPS 提升证明，也不是通关或手机性能。此前满载波动问题仍需留意。无尽倍率是明确调参初值，未完成人工长期无尽平衡；没有增加普通前十二关敌人预算。界面快照保存在本地 `reports/local/qa-v16/`，没有上传图片，也没有新的人工原客户端视觉/听感批准。包体仍较大，未对启动峰值内存给出保证。

实际命令：`npm test`、`npm run typecheck`、`npm run build`、`node tools/qa-v16.mjs`、`node tools/qa-controls-v15.mjs`，以及 `SC2_PERF_EXPANSION=only`、`SC2_PERF_OUT=reports/local/qa-v16-performance` 下的 `node tools/qa-runtime-performance.mjs`（PowerShell 用 `$env:` 设置）。

## 历史 V15：合批增援、五兵种、精英与英雄（2026-09-22）

运行代码 `406b24a7a08cd7edc6e834081eb5ee0906dd3187`；`v3/playable-sc2-demo` / PR #2，目标分支仍为 `v2/sc2-assets-ui`。**340/340 测试、类型检查、构建通过。** 完整规则见 [DESIGN.md](DESIGN.md)，六阶段记录见 [ITERATION_V15.md](ITERATION_V15.md)。

- 三组按参与建筑合批付款、交替兵种与筹资等待，一个批次一个多人仓。已付款容量、同兵种仓阻塞、逐个碰撞安全出舱、仓毁损失/守军留场均有测试。简单普通波次与守军累计减半，普通旧四类预算未增。
- 五类普通席位共 25，15 款唯一精英替换普通队员、同卡晋升、永久死亡；三位英雄额外席位与技能、同名晋升、关间付费复活。情报概率线性成长、预算内敌方精英、四次 Boss 与一次性奖励已接入。
- 10 基础 + 15 精英 + 3 英雄，**28/28 原模型**在浏览器加载；28 套站立/移动/死亡，23 套普攻，4 医疗艇原 Stand Work 治疗，爆虫原自爆死亡。坦克原双态/变形、诺娃 A 组动作与原武器网格已播放。**31/31 图标**解码；精英使用家族原图标，未宣称有 15 套独立原头像。原声音 10 个解码。新增 267 个源依赖哈希核对，必需缺失 0。
- 独立 M3Studio 结构读取：364 模型、4045 骨骼、1272 动作时长，0 差异。素材字节、动作播放与人工还原批准分开；没有新增人工与原 SC2 客户端逐帧对照批准。图片/播放快照仅在本机 `reports/local/qa-v15/`。
- 288 边缘路线、288 坡脚接近、25 人 32 组双向坡道全部通过，0 越界；最长 17.00 秒。合法细窄边带可接入更远网格中心；局部避让不会朝可避开的崖壁转向；受阻 0.6 秒后绕开已就位友军。保持有限转向、真实碰撞和手动架炮，没有瞬移或整体重排。
- 桌面 1x 鼠标/小地图输入观察完成前两关，成功救出 4 人、6 SCV，击杀 5 Drone；第二关结算仍有一只劫掠者仓等待救援。未改 HP/钱包/敌人数。它是输入流程观察，不是人工技术评估或普通难度通关标准。
- 独立夹具核对多人仓持续可见及三处身份/数量/HP/用途，精英选择、三英雄键盘技能、付费复活、四 Boss 预警/奖励。390×844、844×390：摇杆+英雄技能多指操作/取消、手柄方向键技能及菜单隔离、无按钮重叠、无页面滚动/误选文字。非标准手柄校准保存重载通过；未测实物手柄和手机真机。
- 最终 HTML 在断网 `file://` 启动并接收右键移动，HTTP(S) 请求 0，修改状态 Debug 不存在。**372,231,822 bytes / 354.99 MiB**，647 内嵌素材；SHA-256 `941757f306afc3031ea1b0292294ec6cab42ed0c58fd2b3f5a590c9c3b59bc49`。

### 性能与剩余问题

同一桌面 Chrome headless、AMD Radeon/ANGLE D3D11、1440×900、DPR1、均衡画质、无 CPU 节流。每场预热 15 秒、采样 12 秒。旧 `2059e87` 与当前诊断 HTML 使用相同内嵌素材。高 HP、补充敌军、禁敌人武器伤害/胆汁/资源卡掉落的夹具隔离运行成本，不是真实关卡或手机性能。最终交付采样不与其他重测试并发。

| 友军 / 敌军 | 平均 FPS：旧 → 当前 | 当前 P95 ms | draw calls | 每模拟步 / 每渲染调用 ms | 末尾积压秒（采样变化） |
|---|---:|---:|---:|---:|---:|
| 20 / 100 | 31.03 → 43.94 | 33.3 | 191 | 1.69 / 7.42 | 0.017（变化 +0.017） |
| 25 / 300 | 27.60 → 36.37 | 33.5 | 190 | 5.67 / 10.48 | 0.017（变化 +0.016） |
| 28 / 300，含 15 精英 + 3 英雄 | 无同阵容旧版本 → 30.91 | 49.9 | 273 | 9.26 / 13.22 | 5.400（变化 +0.217） |

各次样本波动较大，早期同机共同阵容约 43/43 FPS，后续基线约 31/28。共享桌面、驱动与调度影响未被隔离，不能归因成所有场景提速。**新增 28/300 满载场景曾低于 30 FPS 并积压模拟，本轮不宣称满载性能目标稳定达成。** 固定模拟没有丢弃规则时间。最终数据与两组早期对照保留在 [v15-performance.json](../reports/qa/v15-performance.json)。

仍需后续：满载性能、原客户端人工视觉/听审、完整人工 30 分钟普通流程、实物手柄。原粒子时序、门骨骼、PBR、英雄技能表现有网页适配；原字体仍缺、采用系统回退。大 HTML 初次加载/内存开销仍高。本机素材哈希与浏览器路径通过，未宣称干净新电脑的首次下载已经实测。

### 数值与命令

[30 分钟条件账本](../reports/balance/v15-envelope.json)明确记录拾取/救援成功率、每关 SCV 假设、无折扣的指定科技与建筑、伤亡及普通培养。普通投入路线第 12 关所需有效单体射击时间约 43%（有限溅射假设 37%），漏经济路线约 93%（79%）；不是胜率，卡牌出现与救援完成都没有被证明必然发生。没有借精英增强继续加怪。两档完整 1800 秒阶段/暂停/结算测试是规则夹具，禁用战斗后检查状态流，绝非自动策略通关。

实际运行：`npm test`、`npm run typecheck`、`npm run build`、`node tools/qa-expansion-v15.mjs`、`node tools/qa-controls-v15.mjs`、`node tools/qa-opening-v15.mjs`、`node tools/qa-gamepad-calibration.mjs`、`node tools/qa-squad-rewards.mjs`、`node --import tsx tools/audit-map-navigation.mts`（`SC2_MAP_AUDIT_SHOULDERS=1`）、`node --import tsx tools/audit-map-crowding.mts`、`node --import tsx tools/budget-envelope.mts`、`python tools/audit-m3-independent.py`、`pwsh -NoProfile -File tools/fetch-expansion-casc.ps1 -VerifyOnly`。性能使用 `node tools/build-performance-fixtures.mjs 2059e87` 和 `SC2_PERF_URL` 指定冻结文件后运行 `node tools/qa-runtime-performance.mjs`，扩展场景 `SC2_PERF_EXPANSION=only`。

文本证据：[模型/仓/英雄/Boss/离线](../reports/qa/v15-runtime.json)、[触屏/手柄](../reports/qa/v15-controls.json)、[前两关输入](../reports/qa/v15-opening.json)、[边缘/坡脚](../reports/qa/v15-navigation.json)、[整队通道](../reports/qa/v15-crowding.json)、[原骨骼动作](../reports/qa/v15-models.json)、[状态](../reports/STATUS.json)。

## 历史 V14：帧数与玩家小队 AI（2026-09-22）

运行代码 `653a4dad2cfbca6bcabddaf07bc97abfb45fe9e3`，继续 `v3/playable-sc2-demo` / PR #2。**287/287 测试、类型检查、构建通过。** 本轮未改模型/贴图质量、固定步长、数值、波次或经济；所有规则积压仍保留。

- 修复“空目标”逐帧重搜绕过思考间隔的问题；先比较目标分数，再检查地形视线；同一步共享友军编队规划。新玩家命令立即受理，仍需要合法射程、有限瞄准与出弹时间。
- 可动队员追击远处指定目标时可短停还击可攻击的近处威胁，指定目标可攻击后立即优先。远目标不吞已承诺的出弹。尚未到场的队员也参与集火展开；恶火仍避免为后方自动目标反复掉头。行军不先倒退进入后排编队槽。
- 医疗艇优先治疗射程内合法伤员，对微小 HP 差保持当前治疗束；移动时跟随并继续范围内治疗。连续治疗、能量消耗、机械维修限制和手动架炮保持不变。
- 实例只上传活动区间、空批次停止提交，原地图以完整模型包围范围裁剪屏幕外实例。修复裁剪改变透明批次排序的问题。三个尺寸、原点及16坡道共 **51组静止帧与全量已开放地图逐像素完全相同**（差值0）；参考是同一网页渲染器，不能视作原SC2客户端视觉批准。
- **20人、32组双向坡道通过，0越界**；最长9.77秒、平均7.04秒。上一版对应8.12/6.97秒，本轮不宣称通道行军全面加快。距自身合法目标3单位内的既有判据不变。288边缘/288坡脚的静态路由结果沿用V13（地形数据及静态路由未改）。
- 实际桌面鼠标：移动途中开火、左右键追击、指定目标可攻击后的优先级、目标移动/死亡、连续指令、医疗选择及行军、模拟手柄接管通过。390×844/844×390：轻点、摇杆+架炮、多指取消、奖励多买/刷新/维修/拾取通过，0选中文字/页面滚动/浏览器错误。未做手机真机或实物手柄测试。
- 正式HTML在断网 `file://` 启动、右键移动与键盘接管通过，HTTP(S)请求0、修改状态Debug不存在。**176,532,847 bytes / 168.35 MiB**，581内嵌素材；SHA256 `cfadda2bb0584751c233832d10c60c4f621d9913bb933e92c182e21f7f23a375`。
- 本轮实际加载战斗模型8/8、原动作8套、死亡8套、经济模型3、资源模型3、仓体、地图模型292；图标23/23解码。声音10个在后续战斗快照解码完成（离线启动早期快照只有4个，属异步加载时点）。不增加人工外观/听感批准。

### 最终性能证据

为贴近双击HTML场景，以旧提交 `cca5e34` 和当前源码分别制作**相同内嵌资源、压缩方式、CSS的本地诊断HTML**；仅诊断副本带API，正式Demo无该接口。桌面Chrome headless、AMD Radeon集显/ANGLE D3D11、1440×900、DPR1、均衡、无CPU节流；每场预热15秒、采样12秒，前后顺序运行。夹具高HP、补充敌人、零敌人武器伤害、禁胆汁；不是真实关卡或通关。

| 指标 | 20友军 / 100敌军：旧→新 | 25友军 / 300敌军：旧→新 |
|---|---:|---:|
| 平均 FPS | 27.47 → 25.31 | 28.50 → 31.04 |
| 帧时间 P95 | 64.8 → 50.2 ms | 50.1 → 50.0 ms |
| 每模拟步耗时 | 3.68 → 3.37 ms | 7.79 → 6.27 ms |
| 每渲染调用耗时 | 21.41 → 15.41 ms | 16.99 → 11.51 ms |
| draw calls（末帧） | 320 → 189 | 318 → 192 |
| 末尾模拟积压 | 0.00003 → 0.01693 s | 3.83337 → 0.01657 s |

**重负载吞吐改善，轻负载FPS没有改善。** JS调用耗时含浏览器/驱动等待，不能当硬件CPU周期计数。场景间不宜按敌人数直接比较：交战、爆虫死亡特效与拥挤状态不同。帧数有波动，不宣称全场景提速或稳定60FPS。

开发版同条件基线37.78/29.75FPS；最终两次20/100为34.20/31.08，25/300为32.12/31.41。中途37.76FPS重负载样本早于透明排序修复，不作为最终结果。各次原始报告都保留；规则计时没有跳帧或降速。

文本证据：[性能全部样本](../reports/qa/v14-performance.json)、[地图像素对照](../reports/qa/v14-culling.json)、[指令/医疗/离线](../reports/qa/v14-commands.json)、[奖励/图标/触屏](../reports/qa/v14-squad-rewards.json)、[满编通道](../reports/qa/v14-crowding.json)、[交付状态](../reports/STATUS.json)。截图和诊断HTML仅在本地 `reports/local/qa-v14-*`，不上传、不入库。

实际命令：`npm test`、`npm run typecheck`、`npm run build`；`node --import tsx tools/audit-map-crowding.mts`；`node tools/qa-map-culling.mjs` + `python tools/compare-map-culling.py`；`node tools/qa-command-engagement.mjs`（`SC2_QA_OFFLINE=1`）；`node tools/qa-squad-rewards.mjs`；`node tools/qa-runtime-performance.mjs`；`node tools/build-performance-fixtures.mjs cca5e344fd9231eb0243d785eac22b3397e6de7d` 后通过 `SC2_PERF_URL` 指定两个本地file URL顺序复测（输出目录由 `SC2_PERF_OUT` 指定；PowerShell使用 `$env:`）。本地对照需要 Pillow，诊断导出使用现有 esbuild/Git/tar。

仍未完成完整人工30分钟、手机真机、实物手柄与原客户端逐帧对照。原地图材质/粒子等适配限制及大文件加载仍存在；轻负载FPS收益尚未确认，下一次优化应继续以实际帧时间剖面定位。

## 历史 V13：横向展开、机械维修与稀有支援（2026-09-22）

运行代码 `9023b71`，继续原分支与 PR #2。**274/274 规则测试、类型检查、构建通过。** 规则、输入与通行诊断不作为自动策略通关。

- 开阔地最多三列，狭窄处使用各自可走目标；多名射手向两侧展开，进入射程即可开火。八种战斗模型、挂点和身体半径使用0.8游戏比例，原地图、移速、射程不变。15名混编地面队员诊断均能开火；不瞬移、不自动收炮。
- 两档关卡均为60/120/180/240秒四组、共30分钟；敌人总量、守军、属性和通关收入不变。掉落与每SCV增量+15%。建成重工厂直接产恶火，实验室只解锁坦克，误导提示已修正。
- 纳米维修升级允许医疗艇持续耗能维修合法机械队员；不能治疗自己、建筑、敌人或死者。实际DOM购买后，HP增加与能量下降均核对。
- 白/绿/蓝/紫/橙为64/20/10/5/1%；绿直接Rank3、蓝直接Rank5，保留容量、伤损与冷却。紫/橙为有效强化，特殊单位/英雄/装备暂缺。地图支援更低频率、拾取只结算一次，暂停和隔崖不可吸取。多买、50+40n刷新与经济卡正收益保留。
- **288条边缘、288次坡脚接近、20人32组双向坡道全部通过，0越界。** V12的7个堵点在本轮诊断消失。最长8.12秒、平均6.97秒，最大单步0.126。定义为距各自合法目标3单位内，不宣称所有实战会车均已覆盖。静态连接图和路由缓存避免反复全图搜索，仍检查坡道与身体半径。
- 桌面1440×900及390×844、844×390：实际点击购买、品质颜色、刷新、暂停冻结、维修、地图拾取、追击/移动开火、触摸摇杆+架炮与取消通过；0滚动/文字选择/浏览器错误。手柄接管用模拟API；未测手机真机或实物手柄。
- **断网file://**开始、右键移动和键盘接管通过，HTTP(S)请求0，修改状态Debug不存在。HTML **176,530,947 bytes / 168.35 MiB**，581内嵌素材；SHA256 `94c1eabc252c96f1a6afdbcc4fd2cf7f1939f738dd3f4dd0ba6b2faa6813c86a`。
- 战斗模型**8/8**、原动作**8套**、死亡**8套**、图标**23/23**实际加载/解码；经济模型3、资源模型3、仓体、原声音10、地图模型292可用。这些不是原客户端逐帧视觉或听感批准，本轮未新增人工批准。截图只存本地。

桌面独立性能：Chrome、AMD Radeon集显/ANGLE D3D11、1440×900、DPR1、均衡、无CPU节流，预热15秒后采样12秒。20/100 **33.87 FPS**，P95 49.9ms，319 draw calls，积压0.017秒；超编25/300 **26.88 FPS**，P95 50.2ms，321 draw calls，积压从1.700降到1.283秒，**仍未达30FPS目标**。夹具高HP、补敌、禁胆汁，不是真实关卡或手机性能；固定规则步没有跳过。

[30分钟账本](../reports/balance/v13-envelope.json)明确假设SCV、拾取、救援成功率、伤亡和指定原价卡。普通常规投入第12关需约51%理论有效射击时间（已计护甲与属性伤害）（假设有效溅射45%），漏经济路线64%/58%；这是可行性上界，不含稀有强化/地图支援，绝非实际通关。没有按脚本失败加保底。完整人工30分钟流程仍未验证。

文本证据：[奖励/触屏/离线](../reports/qa/v13-squad-rewards.json)、[指令](../reports/qa/v13-commands.json)、[边缘/坡脚](../reports/qa/v13-navigation.json)、[满编](../reports/qa/v13-crowding.json)、[负载](../reports/qa/v13-load.json)、[状态](../reports/STATUS.json)。截图在本地`reports/local/qa-v13-*`。

实际命令：`npm test`、`npm run typecheck`、`npm run build`、`node --import tsx tools/budget-envelope.mts`、`node --import tsx tools/audit-map-crowding.mts`、`node --import tsx tools/audit-map-navigation.mts`（`SC2_MAP_AUDIT_SHOULDERS=1`）、`node tools/qa-squad-load.mjs`、`node tools/qa-squad-rewards.mjs`、`node tools/qa-command-engagement.mjs`（两浏览器脚本用`SC2_QA_OFFLINE=1`；PowerShell以`$env:`赋值）。

## 历史 V12：小地图、高低地和地图审计（2026-09-22）

运行代码 `2f22af9`。**253/253 单元／规则测试通过，类型检查、构建通过；满编拥堵诊断尚未全部通过。**

- 新增战术小地图，实际鼠标右键移动和左右键敌方集火接敌有效；390×844与844×390触屏轻点通过，与摇杆／技能／兵种栏无覆盖，0文字选中／滚动。暂停隐藏小地图，不透传命令。没有手机或实物手柄测试。
- 原 Kairos 高低地：枪兵在2单位高差崖边，两秒对低地蟑螂造成12实际伤害；隔崖近战仍禁用，独立更高山脊仍阻挡。
- 修复三关坦克边缘路线卡点；288条单兵边缘路线、288次坡脚直行／斜向／双向测试通过，0越界。真实World四兵种往返1/3/6/9/12关最远点共10段通过。
- **满编20人停靠坡口：32场景中7个90秒后仍有坦克明显偏离自身目标（>3单位），未通过。** 3号反向、4号双向、8号反向、9号反向、13号双向。正常队尾停在坡下不算失败。这个诊断明确与单兵／四人结果分开，不声称全体量寻路正常。
- 地表8层独立重复/mip，保留原像素；移除整块陡角删面造成的坡脚洞。51,072三角形、25,857顶点，无非有限坐标或WebGL错误；16坡道截图在本地。没有人工像素一致性批准，不能承诺画面无任何异常。
- 地图保持1:1。单枪兵到首关最远测试边缘8.10秒，末关52.48秒；无交战四兵种末关去程46.68秒。实战交火／手动架炮可能更慢，没有因地图大自动加速或减少敌人。
- 最终单HTML **176,519,356 bytes / 168.34 MiB**，581内嵌素材，SHA-256 `83dfbd65fcf22e0e8be36e9456cdd9eee9322079b48ffa3cfb3eeaefee23d734`。断网file://启动和小地图下令通过，HTTP(S)请求0；8战斗模型、292地图模型载入。模型/图标/声音本轮不做新的人工外观或听感宣称。

命令：`npm test`、`npm run typecheck`、`npm run build`；`node --import tsx tools/audit-map-navigation.mts`（边缘+直行，坡脚侧向用SC2_MAP_AUDIT_RAMPS_ONLY=1及SC2_MAP_AUDIT_SHOULDERS=1）；`node --import tsx tools/audit-map-squad.mts`；`node --import tsx tools/audit-map-crowding.mts`（已知失败）；`SC2_QA_OFFLINE=1 node tools/qa-map.mjs`。PowerShell环境变量用 `$env:` 赋值。

文本证据：[浏览器与离线](../reports/qa/v12-map.json)、[通行](../reports/qa/v12-navigation.json)、[混编与高低地](../reports/qa/v12-squad.json)、[满编堵点](../reports/qa/v12-crowding.json)。截图仅 `reports/local/qa-v12-map/`。旧性能数字不代表新增小地图/材质后的测量。

## 前一轮 V11：左键自动寻路集火（2026-09-22）

运行代码 `e5b472a`。删除误加的左键站定模式，左右键点敌共用寻路接敌／优先集火，目标移动继续追踪，死亡后恢复自动索敌。界面与规则同步更正。

- `npm test` **246/246 通过**；`npm run typecheck`、`npm run build` 通过。替换站定断言，覆盖主动接敌、移动目标、绕崖经坡道进入射程；保留移动射击、连续指令周期、手动架炮、治疗和手柄接管回归。
- 实际桌面左键点击远处敌人：锚点和单位均移动超过2单位后造成伤害；目标再次移开后继续追踪开火；指定目标死亡后重新攻击附近敌人。右键、HUD、连续点击、手柄转鼠标首击、390×844／844×390触控仿真继续通过。没有实物手柄或手机真机测试。
- 断网file://启动及操作通过，HTTP(S)请求0；最终HTML **176,510,352 bytes / 168.33 MiB**，SHA-256 `33cb281aca6794e987812728d6f69200d381a9b625ad751fdca764064abf2fa4`，581内嵌素材。
- 浏览器命令：`SC2_QA_OFFLINE=1 node tools/qa-command-engagement.mjs`（PowerShell环境变量赋值）。[文本证据](../reports/qa/v11-left-focus.json)；截图仅在 `reports/local/qa-v11-left-focus/`。下方站定行为为已被本轮取代的历史记录。

## 前一轮 V10：玩家指令与索敌（2026-09-22）

运行代码 `c0818ff`；**246/246 测试、类型检查、构建通过**。右键前进交战／追击集火，左键站定集火；没有更改单位数值、波次、经济和素材。

- 新增11项规则回归，修复前其中10项复现失败，修复后全部通过。覆盖枪兵／恶火／坦克前进时开火、未到编队槽位仍还击、连续点击不吞出弹或重置周期、左键保持站位并优先指定目标、射程外或隔崖目标下的合法本地还击、重新转回指定目标、死亡后继续索敌、坦克射程盲区、医疗艇移动命令期间治疗。保留恶火行军不为后方自动目标反复掉头与手动架炮。
- 实际桌面 Chrome 鼠标事件：右键移动途中产生伤害；模拟槽位阻塞仍开火；左键集火保持锚点和士兵位置、优先玩家目标；左键地面和HUD不覆盖集火；指定目标离开／进入射程和死亡后自动还击；连续右键不中断出弹；右键敌人追击。手柄API模拟后，第一条鼠标指令保留，未被接管清理吃掉。
- 390×844、844×390 CDP轻点地面交战／点敌追击通过，无选中文字和页面滚动。本轮没有手机真机或实物手柄测试。完整多点触摸历史证据仍见V9，不冒充本轮新增验收。
- 最终HTML **176,510,665 bytes / 168.33 MiB**，581内嵌素材。断网file://启动、右键指令和键盘接管通过，HTTP(S)请求0，8/8战斗模型加载、无修改状态Debug接口。SHA-256 `88eec89616515ec29c3031ed5280b49f36d3718d989d7efbdea70dcb27cb5795`。
- 本轮只验证操作，不运行自动通关，不调整普通难度。没有重测性能、完整人工流程或原客户端视觉；下方V9负载与首关观察均为历史版本。

命令：`node --import tsx --test test/command-engagement.test.ts test/commands.test.ts test/gamepad.test.ts`、`npm test`、`npm run typecheck`、`npm run build`、`SC2_QA_OFFLINE=1 node tools/qa-command-engagement.mjs`（PowerShell用环境变量赋值）。

[公开文本证据](../reports/qa/v10-engagement.json)；日志和截图仅保存在 `reports/local/qa-v10-engagement/`、`reports/local/tests-v10.log`、`reports/local/build-v10.log`，不作为人工视觉批准。

## 前一轮 V9：原地图、资源、多买与手柄

运行代码 `0e43fad`，分支 `v3/playable-sc2-demo`，继续更新 PR #2。**235/235 测试、类型检查、构建通过**。以下旧版记录保留历史版本标签，不作为新版实测。

- 真实 Kairos Junction LE，12个递增连通范围，原比例1:1。292地图模型、4,956模型摆放、2,473崖壁、179共享图片；无缺失运行素材。511源文件哈希锁定，本地完整管线重跑通过。四兵种跨原坡道行军120模拟秒诊断，最后编队槽误差约0.08单位；不评价普通难度通关。
- 原 Crystal / GasCanister / SpaceMineralCluster 3/3；战斗模型8/8、原动作8套、死亡8套、经济模型3/3、原仓体、图标23/23解码、原声音10/10解码、效果贴图33张。**这些是来源/加载/播放与数值证据，不是人工视觉批准。** 原图中立装饰不再误套敌方队伍色，并使用原摆放色调。
- 标准手柄模拟：部署、自动选敌、扳机换目标、松杆追击、暂停、多买、售罄禁用、刷新后焦点、两轮90矿共享费用、B聚焦继续、断连清输入并暂停、回中立后明确恢复。键盘接管后，即使旧手柄仍压着摇杆也不会抢回输入。非标准手柄模拟：自定义面键/摇杆/轴式扳机校准、本地保存、重载后使用。没有真实手柄硬件测量。
- 实际鼠标右键移动/集火、伤害与目标一致、键盘接管、HUD不透传、暂停冻结、高差拾取通过。地图扩展后检查97组崖壁实例，过期拾取范围0；更新实例时清除旧包围缓存。390×844与844×390的CDP触摸：轻点、拖动/长按/双指防误发命令、摇杆+架炮、取消/释放、两轮继续、无选中文字/页面滚动均通过；它们不是手机真机测试。
- 最终离线HTML断网file://载入并操作，HTTP(S)请求0，8原战斗模型、3资源模型和292地图模型载入，开发修改接口不存在。**176,509,513 bytes / 168.33 MiB**，581内嵌资源；SHA-256见STATUS。

桌面独立负载：Chrome、AMD Radeon集显/ANGLE D3D11、1440×900、DPR1、均衡画质、无CPU节流，25友军/300敌军诊断。最终运行代码预热5秒后采样464帧，平均 **38.61 FPS**，P95 **33.5 ms**，**317 draw calls**，结束模拟积压 **0.2334秒**。诊断使用高HP、补充敌人、关闭胆汁；不是自然波次或通关结果。较早独立样本45.31 FPS保留在Git历史，本次交付采用最终代码的最新样本；不把有并发测试干扰的样本计入结论。证据见v9-load。

真实时间首关观察：旧键盘策略约62.38秒失败，击杀4、救SCV1、失败仓1、无人存活；未修改HP/钱包/出生或时间。该旧策略没有完成首关，不能把它写成成功验收，也不据此弱化普通难度。关间结算、多买/刷新/跳过通过独立场景验证。完整人工48分钟流程、原客户端逐像素/逐格对照、真手柄和本轮手机真机仍未验证。

文本证据：[操作/离线](../reports/qa/v9-controls.json)、[标准手柄](../reports/qa/v9-gamepad.json)、[校准](../reports/qa/v9-gamepad-calibration.json)、[原图与寻路](../reports/qa/v9-map-routes.json)、[首关观察](../reports/qa/v9-opening.json)、[独立负载](../reports/qa/v9-load.json)、[完整状态](../reports/STATUS.json)。截图仅在 `reports/local/qa-v9*`。原客户端粒子、动态材质、风动、部分占地代理与字体边界见 [MAP_PIPELINE.md](MAP_PIPELINE.md)。

实际命令：`npm run assets:map`、`npm test`、`npm run typecheck`、`npm run build`、`node --import tsx tools/qa-map-routes.mjs`、`node tools/qa-gamepad-shop.mjs`、`node tools/qa-gamepad-calibration.mjs`、`node tools/qa-v9-opening.mjs`、`node tools/qa-v9-load.mjs`、`node tools/qa-v9-controls.mjs`（含 `SC2_QA_OFFLINE=1`）。


2026-09-21 · 运行代码 `30e173ad9346a111ac9dfb5e752c1d06a62dced4` · `D:\星际` · `v3/playable-sc2-demo` · [PR #2](https://github.com/ziyuuu/sc2-survivors-/pull/2)。当前数值、旧版真机与人工视觉结论分开记录。

## 点选操作验收

- 电脑实际鼠标右键地面移动、右键敌人集火通过；左键和 HUD 不发指令，右键菜单被阻止。键盘方向立即接管。暂停保持命令且模拟时间冻结，目标死亡解除集火。实际高台拾取使用高度 3 的落点，不投影到下方平地。
- 浏览器 390×844、844×390 的 CDP 触摸事件：轻点移动／集火通过，拖动、长按、取消、双指战场触摸不下令；摇杆＋架炮按钮同时有效，松手后方向归零，0 文字选中、0 页面滚动，两轮跳过仍可操作。这是桌面触屏仿真，本次没有荣耀真机测试。
- 新增 17 个规则／拾取测试：真实跟随与到位自动战斗、混编射程、Medivac 合法治疗、部署坦克等待手动收炮、替换目标保持冷却、重复集火不打断出弹、新救援兵继承移动、坡道／障碍路由、触控容差不改实体半径、垂直崖壁不穿透点选。
- 断网单文件再次实际右键移动、键盘接管通过，0 网络请求、0 浏览器／渲染错误。绿色移动圈与红色目标圈已生成本地截图；没有以截图保存宣称人工视觉批准。
- 当前回归仅验证输入与规则，不进行自动策略通关，不修改难度或经济预算。此前自然首关记录、压力与真机样本保持历史标签。
- 原始记录：`reports/local/qa-v8-commands/REPORT.json`；可公开文本：[v8-commands.json](../reports/qa/v8-commands.json)。执行：`node tools/qa-point-commands.mjs`，离线复验时设置环境变量 `SC2_QA_OFFLINE=1`。

## 前一轮试玩反馈的处理

- 前两关首批在 5–7 秒生成，约 9–10 秒一批。第一关 12 次单只跳虫，第二关普通 13 次双只；简单第二关 19 只分到 13 次。两关仍为 120 秒；每仓守军、单只 HP、每次掉落和通关奖励不变，第三关以后波量不变。
- 已升级重工厂筹备有容量的坦克。下一个订单成本保留在可见钱包中，其他建筑只用真实余量，付款后轮转；关间卡牌可以使用该钱包，未开工不扣款。星港与坦克实验室没有先后依赖。
- 车辆按制动距离刹停，有限转向／实体碰撞保留；坦克炮塔冷却中提前瞄准，车辆不再停在编队槽位却够不到近处敌人。架炮不移动，收炮后寻路针对当前指向点。
- 原 Char 纹理增加高台／平地差异、崖脚阴影、坡道铺面和岩石连续底床。坡道和障碍位置读取共享地图，未改变可通行性。

## 测试、构建与离线

- `npm test`：**221 通过，0 失败**；`npm run typecheck` 和 `npm run build` 退出 0。Vite 仍提示大 JS 块。
- 单文件 **80,873,870 bytes / 77.13 MiB**，102 个内嵌资产；SHA-256 `922011ecbf6d63a7b89e5afea9f6f1e0bf68adc86beb523750f820eb90c9461e`。
- 断网 `file://` 启动：0 HTTP(S) 请求；8 个战斗模型、原动作及 8 个死亡模型、原仓体、3 个经济模型载入；10 个原声音全部解码。朋友版没有修改状态的 Debug API。
- 新回归覆盖低速靠点／反向转弯／部署坦克绕行／过期指令、炮塔冷却中瞄准、射程外接敌、高级单位资源逐步到账不会被低级兵耗尽、跨建筑轮转、容量释放、暂停与钱包消费、两关小波次和最大空档。

## 历史桌面记录（bf486d8）

Chrome 1440×900 原生 DPR。正常时间和真实键盘输入，未改 HP、钱、兵力、出生或时钟；策略脚本只选择移动方向。该观察不作为普通难度门槛。

- 首关 120 秒进入关间，普通波次生成 **12** 只，成功救援 **3** 个仓，救出 **2** 个 SCV，击杀 **2** 个 Drone。
- +80 矿／+30 气通关奖励显示并到账，建筑轮／随机轮分别跳过后进入第二关。
- 独立诊断覆盖升级卡实际付款 50/25、星港／实验室独立解锁、T 架起后移动锚点不带走坦克、收起后回归当前目标、SCV 语音一次播放、12 个射击声占用时仍播放救援语音、治疗短断不重启循环和暂停立即停声。
- 23/23 原图标实际浏览器解码。东西坡道、北部坡道和岩石通道均生成本地截图，着色器错误 0；颜色／高度属性与共享地图核对通过。没有宣称人工对照 SC2 视觉通过。

3 米直行到点并停稳，恶火 **2.28→0.73 秒**、坦克 **3.02→1.05 秒**；背向起步分别 **3.20→1.17**、**4.15→1.67 秒**。这是隔离控制对照，基础移速／武器数值未增加。完整条件见 [手感测量](../reports/qa/v7-handling.json)。

## 素材与动画

- Manifest **102/102** 可用，8/8 战斗模型与主动画、8/8 原死亡模型、3 个经济模型、仓体，23 个原图标、33 张效果纹理、8 张地表／崖壁纹理。
- 本次补齐 **10 个原声音 + 2 个武器效果源文件**。SCV 是原简体中文 Ogg；9 个原 WAV 为 IMA ADPCM，在本地转 PCM16，同采样率／声道且逐样本读回相等；原文件保留。浏览器实际播放事件与解码通过，不是人工听感批准。
- 枪兵使用取得的 Fireball_1Hot 枪口层，恶火使用自身 HellionBeam / Flame2；伤害仍只来自模拟。粒子摆放和时序是网页适配，不宣称完整复刻 SC2 粒子引擎。
- 浏览器回放八套移动、七类有武器单位的攻击事件、八套死亡、坦克两形态与变形、医疗 Stand Work 和连续 HP／能量变化；错误 0。原模型独立 M3Studio 核对 24 模型／995 骨骼／208 主片段的旧审计保持有效；本次没有修改其骨架。
- 准确来源、散列和落盘位置见 [原素材清单](ASSET_DOWNLOAD_REQUIRED.md) 与 [文本证据](../reports/qa/v6-original-sources.json)。原包、转换产物与截图均未提交公开仓库。

## 性能与手机测试范围

按最新要求，**前一轮节奏／制动／筹备预算／地形变更未追加真机或手机尺寸测试**。此前的 390×844／844×390 和荣耀横竖屏记录是历史基线，不能冒充当前版本验收。

前一轮桌面负载（bf486d8，本次未重新采样）：Chrome / ANGLE (AMD, AMD Radeon(TM) Graphics (0x00001638) Direct3D11 vs_5_0 ps_5_0, D3D11)，1440×900，DPR 1，均衡，无 CPU 降速。25 友军、约 300 敌军的持续补充诊断，关闭胆汁并提高 HP，预热 5 秒后采样约 12 秒。

| 平均 FPS | P95 ms | Draw calls | 采样帧 | 模拟推进秒 | 末尾积压秒 |
|---:|---:|---:|---:|---:|---:|
| 57.1 | 33.0 | 36 | 685 | 11.65 | 3.08 |

压力场景仍有模拟积压；保留固定 tick、不丢规则时间，不把渲染 FPS 当实时推进证明。诊断与自然关卡、手机性能无等价关系。

历史真机：HONOR AMG-AN00 / Android 16 / SM8650 / Adreno 750 / Chrome 148，均衡 DPR 1.5，每场预热 5 秒后完整 60 秒采样。开局竖／横 59.9 / 59.8 FPS；25/300 诊断 58.0 / 55.5 FPS，末尾积压 2.64 / 5.11 秒。10 个声音解码及两指输入通过；这是最新反馈前的快照，无人工听感或长期温升结论。[历史真机摘要](../reports/qa/v6-phone-summary.json)。

## 边界与复现

原字体、完整粒子／折射／动态材质、人工视觉批准和完整人工 48 分钟流程仍未完成。原门骨骼开舱及已标注死亡变体保持网页适配。请求补充的 10 个声音和两个效果源文件已不再缺失。

实际命令：`npm test`、`npm run typecheck`、`npm run build`、`npm run assets:originals`（另执行 `-Refetch` 与本地转换）、`node tools/import-m3-pack.mjs fx.muzzle fx.flame`、`node tools/qa-fixes-v6.mjs`（本轮设置 `SC2_QA_DESKTOP_ONLY=1`）、`node tools/qa-pace-desktop.mjs`、`node tools/qa-effects.mjs`、`node tools/qa-navigation-load.mjs`、`node --import tsx tools/handling-profile.mts`、`node --import tsx tools/budget-envelope.mts`。本次新增点选操作另执行 `node tools/qa-point-commands.mjs`（含 `SC2_QA_OFFLINE=1`）；只做触屏仿真，不执行真机脚本。

公开摘要：[v7-summary.json](../reports/qa/v7-summary.json)、[STATUS.json](../reports/STATUS.json)。原始图像和日志仅在 `reports/local/qa-v7-desktop/`、`qa-v7-fixes/`、`qa-v6-assets/` 与 `qa-v6-load/`；历史失败／中断日志不计入通过样本。条件经济账本见 [BALANCE_12_STAGES.md](BALANCE_12_STAGES.md)，不是自动通关证据。
