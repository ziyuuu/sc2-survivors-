# 简单／普通试玩验收

运行代码：f3803724f69711173562144f0feb45c675daac80。项目 D:\星际，分支 v3/playable-sc2-demo，沿用 PR #2；main 与旧 V2 保留。测试基准为最新确认的 48 分钟规则：自动付费生产、降落仓无超时、SCV 虫卵 30 秒、收费三选一和实体碰撞。

## 规则、构建与离线

- npm test：160 通过、0 失败。包括攻击周期、护甲/HP、治疗、爆虫范围伤害、走射与拖尾、坦克变形、碰撞/空地分层/窄口、自动扣费与容量、无超时降落仓、工兵期限、付费折扣/跳过/刷新、两档预算和结算。
- npm run typecheck、npm run build：退出 0。构建包含 TypeScript；Vite 仅提示 JS 块超过 500 kB。
- 单 HTML：74,082,133 bytes / 70.65 MiB，84 个内嵌资产。SHA-256：d548e8a9c34d0753f0cc7f3e566af0af1dbc9ce7d4b6aa81366ab87bec5a3ee2。路径：dist/SC2-Survivors-Demo.html。
- Chrome 设置离线后通过 file:// 打开并开始游戏：8/8 战斗模型、3 个经济模型、原降落仓、8 套原动作和死亡模型载入；0 HTTP(S) 请求，无 F1 修改状态 API。
- 两档均走完 2880 秒的规则流程夹具，暂停不计时，最终虫巢有胜负条件；夹具隔离战斗，不是完整实战通关证据。

## 桌面真实时间首关

node tools/qa.mjs 用正常键盘事件跑满 120 秒，没有注入 HP、资源、时间或兵力。脚本选择移动方向，最终进入三选一：击杀 11、救援 3 个仓、救出 2 个 SCV、击杀 2 个 Drone，普通跳虫生成 5 个。
首次刷新实际扣 50 矿、候选组合改变；购买本次“瓦斯补给”收费 22 矿，进入第二关。没有把选卡自动通关率当作普通难度标准。
另开诊断夹具验证舱体毁灭失败、SCV 获救只结算一次、虫卵期限死亡。该夹具直接构造伤害/期限，不能冒充自然实战失败；严格 30 秒期限由固定步长单元测试验证。

## 手机尺寸与桌面清晰度

- 390×844、844×390：摇杆＋推进的多指同时触发、指针移出后松手、选卡点击/滚动均通过；无选中文字、无页面误滚动、无横向溢出。
- 桌面 1440×900 / DPR 1.25 → 1800×1125；1920×1080 / DPR 2 → 3840×2160。断网 HTML 同样保留原生 DPR 与抗锯齿。手机均衡 DPR 上限 1.5；画质保存与暂停冻结通过。
- 8/8 战斗模型实际有原法线与高光层；原 Char 地表/岩石/遗迹载入。没有原字体文件，使用系统回退。字体排版仅做 DOM/滚动检查，截图没有人工视觉批准。

## 性能：模拟与真机分开

桌面 Chrome / AMD Radeon Graphics（Ryzen 7 5800U 主机），手机 DPR 3、渲染 DPR 1.5、4 倍 CPU 降速，25 友军＋300 敌军。预热 6 秒、采样 10 秒，保留最近最多 240 帧；诊断场景补回爆虫等消失单位并提高 HP、关闭胆汁。

| 模拟视口 | 平均 FPS | P95 帧耗时 | Draw calls |
|---|---:|---:|---:|
| 390×844 | 25.6 | 50.1 ms | 35 |
| 844×390 | 27.3 | 50.1 ms | 39 |

这一极端降速模拟仍没有稳定达到 30 FPS。碰撞复用候选、阵营空间索引、缓存关卡数据、原网格 LOD 已接入；没有减少配置敌人数或降低固定模拟频率。

真机：HONOR AMG-AN00，Android 16，SM8650 / Adreno 750，Chrome 148。当前竖屏 CSS 视口约 369×702、渲染 DPR 1.5。通过 USB 访问本地开发版，无 CPU 人为降速。用户解锁后执行，结束已撤销本次 USB 端口映射，没有修改屏幕旋转设置。

| 真机场景 | 活友军/敌军 | 平均 FPS | P95 帧耗时 | 样本 |
|---|---:|---:|---:|---:|
| 开局低负载（尚未接敌） | 1/0 | 54.6 | 16.4 ms | 240 帧 |
| stress-25-100 | 25/100 | 61.2 | 16.4 ms | 240 帧 |
| stress-25-300 | 25/300 | 58.0 | 32.6 ms | 240 帧 |

真机验证移动＋技能同时触发、松手停止、无选中文字、无页面滚动。每场景预热 6 秒后采样 12 秒，最近 240 帧计数；原始 gfxinfo 另存，游戏 FPS 使用网页 rAF 数据，不能混为浏览器原生 UI 帧率。仅一台手机的一次短时测试，未覆盖真机横屏、48 分钟发热或人工手感评价。

## 素材与已知边界

- 84/94 manifest 资源可用、必需缺失 0；8/8 原战斗模型、8 死亡、3 经济模型、原降落仓、23/23 图标、28 原效果贴图、5 原地表贴图。
- 仍缺 10 个原声音（包括 SCV 亮相语音），3 个合成提示音保留；枪口 fireball_1hot 层未取得。准确来源、文件名和放置路径见 ASSET_DOWNLOAD_REQUIRED.md。
- 下落和死亡使用原仓动作；开门由原骨骼适配，没有独立原开舱片段。命中闪光、粒子编排和 PBR 为网页实现，不声称 SC2 客户端完整画质。
- 普通按合理经济/培养的理论可行性设计，不用脚本必须打通作为门槛。数值报告明确抽卡、拾取、救援假设；完整人工通关尚未测试。

## 可复现产物

命令：npm test；npm run typecheck；npm run build；node tools/qa.mjs；node tools/qa-quality.mjs；node --import tsx tools/duel-study.mts；node --import tsx tools/budget-envelope.mts。真机脚本：tools/qa-phone.mjs（指定 SC2_ADB_SERIAL，手机需解锁，使用本次项目标签页和明确 USB 映射）。

可提交的纯文本摘要：reports/qa/v4-summary.json、reports/STATUS.json、reports/balance/v4-envelope.json、v4-duels.json。原始日志、截图、CPU 采样和手机 gfxinfo 在 reports/local 下，仅本机保存。
