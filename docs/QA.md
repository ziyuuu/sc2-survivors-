# 当前迭代验收

## V16：无损离线包、友军辨识与无尽（2026-09-22）

运行代码 `179f00c32dedf5fe2223b73cea26beacf3f7ca89`；分支与 PR #2 的目标保持不变。**351 项测试、类型检查、正式构建通过。** [当前状态](../reports/STATUS.json) / [浏览器记录](../reports/qa/v16-runtime.json)。

- HTML 从 **372,231,822 bytes / 354.99 MiB** 降到 **222,391,244 bytes / 212.09 MiB**，减少 **40.25%**。内容哈希去重掉 56,371,905 字节重复分片，唯一资源 gzip 后 164,334,996 字节，再 Base64 内嵌。全部 647 项原文件在浏览器还原后逐项 SHA-256 相同，未删除、重采样任何原纹理、动作、地图。详细 [包体统计](../reports/qa/v16-pack.json)。
- 正式文件 SHA-256：`ecd120aa7f27649e0a70564aa6c8cf3a0bd5c9b9f1c381e9e103a564908732b0`。断网 `file://` 启动、部署、右键移动通过，HTTP(S) 请求 0，修改状态 Debug 不存在。本次本机冷页面至可部署约 **23.61 秒**，仅一个本机样本，不作为通用启动时长或内存节省承诺。
- 紫色精英战场姓名/军衔、卡牌未拥有/已拥有/待编入、同卡晋升后即时更新均通过。三位英雄的金色名牌、模型高度投影、互不重叠的独立血条、HUD 数值、低血量/阵亡状态已检查；技能无合法目标时仍保持血条明亮。头像与模型保持原资产。
- 1440×900、390×844、844×390 检查可见名称、HP 值、血条宽度、HUD 范围、无页面横向溢出、无浏览器/着色错误。沿用触屏/手柄脚本复测 390×844、844×390 的移动+技能多指操作、取消、方向键英雄技能/菜单隔离。仅尺寸/输入模拟，未做手机真机或实物手柄。
- 桌面点击第十二关胜利后的无尽入口，验证原地图源哈希/面积/模型数、原单位身份/生命及虫巢身份保持；生成小兵、强化精英、Boss，连续建筑/随机选卡后进入无尽第 2 轮。规则测试覆盖解锁限制、30/60/90 秒边界、不同怪物轮换不回落、技能伤害一次缩放、原预警延迟、暂停、重复结算及失败。
- 28 友军/300 敌军场景实际载入 10 基础 + 15 精英 + 3 英雄，渲染错误 0。原动作逐款播放的详细证据保留在 V15；本轮所有动作文件已证明字节未变，不把该事实称为新增人工视觉批准。

### 本轮性能与限制

桌面 Chrome headless / AMD Radeon ANGLE D3D11、1440×900 DPR1、均衡、预热 15 秒、采样 12 秒。28 友军（15 精英与 3 英雄）/300 敌军，平均 **36.90 FPS**、P95 **33.5 ms**、**273 draw calls**、每模拟步平均 **6.59 ms**、每渲染调用 **12.16 ms**、末尾积压 **0.0166 秒**。[原始记录](../reports/qa/v16-performance.json)。采样后仅修改了无尽特殊敌人增长；该普通诊断场景未运行无尽分支。

此样本不构成前后同条件 FPS 提升证明，也不是通关或手机性能。此前满载波动问题仍需留意。无尽倍率是明确调参初值，未完成人工长期无尽平衡；没有增加普通前十二关敌人预算。界面快照保存在本地 `reports/local/qa-v16/`，没有上传图片，也没有新的人工原客户端视觉/听感批准。包体仍较大，未对启动峰值内存给出保证。

实际命令：`npm test`、`npm run typecheck`、`npm run build`、`node tools/qa-v16.mjs`、`node tools/qa-controls-v15.mjs`，以及 `SC2_PERF_EXPANSION=only`、`SC2_PERF_OUT=reports/local/qa-v16-performance` 下的 `node tools/qa-runtime-performance.mjs`（PowerShell 用 `$env:` 设置）。

## 历史 V15：合批增援、五兵种、精英与英雄（2026-09-22）

运行代码 `406b24a7a08cd7edc6e834081eb5ee0906dd3187`；`v3/playable-sc2-demo` / PR #2，目标分支仍为 `v2/sc2-assets-ui`。**340/340 测试、类型检查、构建通过。** 完整规则见 [DESIGN.md](DESIGN.md)，六阶段记录见 [ITERATION_V15.md](ITERATION_V15.md)。

- 三组按参与建筑合批付款、交替兵种与筹资等待，一个批次一个多人仓。已付款容量、同兵种仓阻塞、逐个碰撞安全出舱、仓毁损失/守军留场均有测试。简单普通波次与守军累计减半，普通旧四类预算未增。
- 五类普通席位共 25，15 款唯一精英替换普通队员、同卡晋升、永久死亡；三位英雄额外席位与技能、同名晋升、关间付费复活。情报概率线性成长、预算内敌方精英、四次 Boss 与一次性奖励已接入。
- 10 基础 + 15 精英 + 3 英雄，**28/28 原模型**在浏览器加载；28 套站立/移动/死亡，23 套普攻，4 医疗艇原 Stand Work 治疗，爆虫原自爆死亡。坦克原双态/变形、诺娃 A 组动作与原武器网格已播放。**31/31 图标**解码；精英使用家族原图标，未宣称有 15 套独立原头像。原声音 10 个解码。新增 267 个源依赖哈希核对，必需缺失 0。
- 独立 M3Studio 结构读取：364 模型、4045 骨骼、1272 动作时长，0 差异。素材字节、动作播放与人工还原批准分开；没有新增人工与原 SC2 客户端逐帧对照批准。图片/播放快照仅在本机 `reports/local/qa-v15/`。
- 288 边缘路线、288 坡脚接近、25 人 32 组双向坡道全部通过，0 越界；最长 17.00 秒。合法细窄边带可接入更远网格中心；局部避让不会朝可避开的崖壁转向；受阻 0.6 秒后绕开已就位友军。保持有限转向、真实碰撞和手动架炮，没有瞬移或整体重排。
- 桌面 1x 鼠标/小地图输入观察完成前两关，成功救出 4 人、6 SCV，击杀 5 Drone；第二关结算仍有一只劫掠者仓等待救援。未改 HP/钱包/敌人数。它是输入流程观察，不是人工技术评估或普通难度通关标准。
- 独立夹具核对多人仓持续可见及三处身份/数量/HP/用途，精英选择、三英雄键盘技能、付费复活、四 Boss 预警/奖励。390×844、844×390：摇杆+英雄技能多指操作/取消、手柄方向键技能及菜单隔离、无按钮重叠、无页面滚动/误选文字。非标准手柄校准保存重载通过；未测实物手柄和手机真机。
- 最终 HTML 在断网 `file://` 启动并接收右键移动，HTTP(S) 请求 0，修改状态 Debug 不存在。**372,231,822 bytes / 354.99 MiB**，647 内嵌素材；SHA-256 `941757f306afc3031ea1b0292294ec6cab42ed0c58fd2b3f5a590c9c3b59bc49`。

### 性能与剩余问题

同一桌面 Chrome headless、AMD Radeon/ANGLE D3D11、1440×900、DPR1、均衡画质、无 CPU 节流。每场预热 15 秒、采样 12 秒。旧 `2059e87` 与当前诊断 HTML 使用相同内嵌素材。高 HP、补充敌军、禁敌人武器伤害/胆汁/资源卡掉落的夹具隔离运行成本，不是真实关卡或手机性能。最终交付采样不与其他重测试并发。

| 友军 / 敌军 | 平均 FPS：旧 → 当前 | 当前 P95 ms | draw calls | 每模拟步 / 每渲染调用 ms | 末尾积压秒（采样变化） |
|---|---:|---:|---:|---:|---:|
| 20 / 100 | 31.03 → 43.94 | 33.3 | 191 | 1.69 / 7.42 | 0.017（变化 +0.017） |
| 25 / 300 | 27.60 → 36.37 | 33.5 | 190 | 5.67 / 10.48 | 0.017（变化 +0.016） |
| 28 / 300，含 15 精英 + 3 英雄 | 无同阵容旧版本 → 30.91 | 49.9 | 273 | 9.26 / 13.22 | 5.400（变化 +0.217） |

各次样本波动较大，早期同机共同阵容约 43/43 FPS，后续基线约 31/28。共享桌面、驱动与调度影响未被隔离，不能归因成所有场景提速。**新增 28/300 满载场景曾低于 30 FPS 并积压模拟，本轮不宣称满载性能目标稳定达成。** 固定模拟没有丢弃规则时间。最终数据与两组早期对照保留在 [v15-performance.json](../reports/qa/v15-performance.json)。

仍需后续：满载性能、原客户端人工视觉/听审、完整人工 30 分钟普通流程、实物手柄。原粒子时序、门骨骼、PBR、英雄技能表现有网页适配；原字体仍缺、采用系统回退。大 HTML 初次加载/内存开销仍高。本机素材哈希与浏览器路径通过，未宣称干净新电脑的首次下载已经实测。

### 数值与命令

[30 分钟条件账本](../reports/balance/v15-envelope.json)明确记录拾取/救援成功率、每关 SCV 假设、无折扣的指定科技与建筑、伤亡及普通培养。普通投入路线第 12 关所需有效单体射击时间约 43%（有限溅射假设 37%），漏经济路线约 93%（79%）；不是胜率，卡牌出现与救援完成都没有被证明必然发生。没有借精英增强继续加怪。两档完整 1800 秒阶段/暂停/结算测试是规则夹具，禁用战斗后检查状态流，绝非自动策略通关。

实际运行：`npm test`、`npm run typecheck`、`npm run build`、`node tools/qa-expansion-v15.mjs`、`node tools/qa-controls-v15.mjs`、`node tools/qa-opening-v15.mjs`、`node tools/qa-gamepad-calibration.mjs`、`node tools/qa-squad-rewards.mjs`、`node --import tsx tools/audit-map-navigation.mts`（`SC2_MAP_AUDIT_SHOULDERS=1`）、`node --import tsx tools/audit-map-crowding.mts`、`node --import tsx tools/budget-envelope.mts`、`python tools/audit-m3-independent.py`、`pwsh -NoProfile -File tools/fetch-expansion-casc.ps1 -VerifyOnly`。性能使用 `node tools/build-performance-fixtures.mjs 2059e87` 和 `SC2_PERF_URL` 指定冻结文件后运行 `node tools/qa-runtime-performance.mjs`，扩展场景 `SC2_PERF_EXPANSION=only`。

文本证据：[模型/仓/英雄/Boss/离线](../reports/qa/v15-runtime.json)、[触屏/手柄](../reports/qa/v15-controls.json)、[前两关输入](../reports/qa/v15-opening.json)、[边缘/坡脚](../reports/qa/v15-navigation.json)、[整队通道](../reports/qa/v15-crowding.json)、[原骨骼动作](../reports/qa/v15-models.json)、[状态](../reports/STATUS.json)。

## 历史 V14：帧数与玩家小队 AI（2026-09-22）

运行代码 `653a4dad2cfbca6bcabddaf07bc97abfb45fe9e3`，继续 `v3/playable-sc2-demo` / PR #2。**287/287 测试、类型检查、构建通过。** 本轮未改模型/贴图质量、固定步长、数值、波次或经济；所有规则积压仍保留。

- 修复“空目标”逐帧重搜绕过思考间隔的问题；先比较目标分数，再检查地形视线；同一步共享友军编队规划。新玩家命令立即受理，仍需要合法射程、有限瞄准与出弹时间。
- 可动队员追击远处指定目标时可短停还击可攻击的近处威胁，指定目标可攻击后立即优先。远目标不吞已承诺的出弹。尚未到场的队员也参与集火展开；恶火仍避免为后方自动目标反复掉头。行军不先倒退进入后排编队槽。
- 医疗艇优先治疗射程内合法伤员，对微小 HP 差保持当前治疗束；移动时跟随并继续范围内治疗。连续治疗、能量消耗、机械维修限制和手动架炮保持不变。
- 实例只上传活动区间、空批次停止提交，原地图以完整模型包围范围裁剪屏幕外实例。修复裁剪改变透明批次排序的问题。三个尺寸、原点及16坡道共 **51组静止帧与全量已开放地图逐像素完全相同**（差值0）；参考是同一网页渲染器，不能视作原SC2客户端视觉批准。
- **20人、32组双向坡道通过，0越界**；最长9.77秒、平均7.04秒。上一版对应8.12/6.97秒，本轮不宣称通道行军全面加快。距自身合法目标3单位内的既有判据不变。288边缘/288坡脚的静态路由结果沿用V13（地形数据及静态路由未改）。
- 实际桌面鼠标：移动途中开火、左右键追击、指定目标可攻击后的优先级、目标移动/死亡、连续指令、医疗选择及行军、模拟手柄接管通过。390×844/844×390：轻点、摇杆+架炮、多指取消、奖励多买/刷新/维修/拾取通过，0选中文字/页面滚动/浏览器错误。未做手机真机或实物手柄测试。
- 正式HTML在断网 `file://` 启动、右键移动与键盘接管通过，HTTP(S)请求0、修改状态Debug不存在。**176,532,847 bytes / 168.35 MiB**，581内嵌素材；SHA256 `cfadda2bb0584751c233832d10c60c4f621d9913bb933e92c182e21f7f23a375`。
- 本轮实际加载战斗模型8/8、原动作8套、死亡8套、经济模型3、资源模型3、仓体、地图模型292；图标23/23解码。声音10个在后续战斗快照解码完成（离线启动早期快照只有4个，属异步加载时点）。不增加人工外观/听感批准。

### 最终性能证据

为贴近双击HTML场景，以旧提交 `cca5e34` 和当前源码分别制作**相同内嵌资源、压缩方式、CSS的本地诊断HTML**；仅诊断副本带API，正式Demo无该接口。桌面Chrome headless、AMD Radeon集显/ANGLE D3D11、1440×900、DPR1、均衡、无CPU节流；每场预热15秒、采样12秒，前后顺序运行。夹具高HP、补充敌人、零敌人武器伤害、禁胆汁；不是真实关卡或通关。

| 指标 | 20友军 / 100敌军：旧→新 | 25友军 / 300敌军：旧→新 |
|---|---:|---:|
| 平均 FPS | 27.47 → 25.31 | 28.50 → 31.04 |
| 帧时间 P95 | 64.8 → 50.2 ms | 50.1 → 50.0 ms |
| 每模拟步耗时 | 3.68 → 3.37 ms | 7.79 → 6.27 ms |
| 每渲染调用耗时 | 21.41 → 15.41 ms | 16.99 → 11.51 ms |
| draw calls（末帧） | 320 → 189 | 318 → 192 |
| 末尾模拟积压 | 0.00003 → 0.01693 s | 3.83337 → 0.01657 s |

**重负载吞吐改善，轻负载FPS没有改善。** JS调用耗时含浏览器/驱动等待，不能当硬件CPU周期计数。场景间不宜按敌人数直接比较：交战、爆虫死亡特效与拥挤状态不同。帧数有波动，不宣称全场景提速或稳定60FPS。

开发版同条件基线37.78/29.75FPS；最终两次20/100为34.20/31.08，25/300为32.12/31.41。中途37.76FPS重负载样本早于透明排序修复，不作为最终结果。各次原始报告都保留；规则计时没有跳帧或降速。

文本证据：[性能全部样本](../reports/qa/v14-performance.json)、[地图像素对照](../reports/qa/v14-culling.json)、[指令/医疗/离线](../reports/qa/v14-commands.json)、[奖励/图标/触屏](../reports/qa/v14-squad-rewards.json)、[满编通道](../reports/qa/v14-crowding.json)、[交付状态](../reports/STATUS.json)。截图和诊断HTML仅在本地 `reports/local/qa-v14-*`，不上传、不入库。

实际命令：`npm test`、`npm run typecheck`、`npm run build`；`node --import tsx tools/audit-map-crowding.mts`；`node tools/qa-map-culling.mjs` + `python tools/compare-map-culling.py`；`node tools/qa-command-engagement.mjs`（`SC2_QA_OFFLINE=1`）；`node tools/qa-squad-rewards.mjs`；`node tools/qa-runtime-performance.mjs`；`node tools/build-performance-fixtures.mjs cca5e344fd9231eb0243d785eac22b3397e6de7d` 后通过 `SC2_PERF_URL` 指定两个本地file URL顺序复测（输出目录由 `SC2_PERF_OUT` 指定；PowerShell使用 `$env:`）。本地对照需要 Pillow，诊断导出使用现有 esbuild/Git/tar。

仍未完成完整人工30分钟、手机真机、实物手柄与原客户端逐帧对照。原地图材质/粒子等适配限制及大文件加载仍存在；轻负载FPS收益尚未确认，下一次优化应继续以实际帧时间剖面定位。

## 历史 V13：横向展开、机械维修与稀有支援（2026-09-22）

运行代码 `9023b71`，继续原分支与 PR #2。**274/274 规则测试、类型检查、构建通过。** 规则、输入与通行诊断不作为自动策略通关。

- 开阔地最多三列，狭窄处使用各自可走目标；多名射手向两侧展开，进入射程即可开火。八种战斗模型、挂点和身体半径使用0.8游戏比例，原地图、移速、射程不变。15名混编地面队员诊断均能开火；不瞬移、不自动收炮。
- 两档关卡均为60/120/180/240秒四组、共30分钟；敌人总量、守军、属性和通关收入不变。掉落与每SCV增量+15%。建成重工厂直接产恶火，实验室只解锁坦克，误导提示已修正。
- 纳米维修升级允许医疗艇持续耗能维修合法机械队员；不能治疗自己、建筑、敌人或死者。实际DOM购买后，HP增加与能量下降均核对。
- 白/绿/蓝/紫/橙为64/20/10/5/1%；绿直接Rank3、蓝直接Rank5，保留容量、伤损与冷却。紫/橙为有效强化，特殊单位/英雄/装备暂缺。地图支援更低频率、拾取只结算一次，暂停和隔崖不可吸取。多买、50+40n刷新与经济卡正收益保留。
- **288条边缘、288次坡脚接近、20人32组双向坡道全部通过，0越界。** V12的7个堵点在本轮诊断消失。最长8.12秒、平均6.97秒，最大单步0.126。定义为距各自合法目标3单位内，不宣称所有实战会车均已覆盖。静态连接图和路由缓存避免反复全图搜索，仍检查坡道与身体半径。
- 桌面1440×900及390×844、844×390：实际点击购买、品质颜色、刷新、暂停冻结、维修、地图拾取、追击/移动开火、触摸摇杆+架炮与取消通过；0滚动/文字选择/浏览器错误。手柄接管用模拟API；未测手机真机或实物手柄。
- **断网file://**开始、右键移动和键盘接管通过，HTTP(S)请求0，修改状态Debug不存在。HTML **176,530,947 bytes / 168.35 MiB**，581内嵌素材；SHA256 `94c1eabc252c96f1a6afdbcc4fd2cf7f1939f738dd3f4dd0ba6b2faa6813c86a`。
- 战斗模型**8/8**、原动作**8套**、死亡**8套**、图标**23/23**实际加载/解码；经济模型3、资源模型3、仓体、原声音10、地图模型292可用。这些不是原客户端逐帧视觉或听感批准，本轮未新增人工批准。截图只存本地。

桌面独立性能：Chrome、AMD Radeon集显/ANGLE D3D11、1440×900、DPR1、均衡、无CPU节流，预热15秒后采样12秒。20/100 **33.87 FPS**，P95 49.9ms，319 draw calls，积压0.017秒；超编25/300 **26.88 FPS**，P95 50.2ms，321 draw calls，积压从1.700降到1.283秒，**仍未达30FPS目标**。夹具高HP、补敌、禁胆汁，不是真实关卡或手机性能；固定规则步没有跳过。

[30分钟账本](../reports/balance/v13-envelope.json)明确假设SCV、拾取、救援成功率、伤亡和指定原价卡。普通常规投入第12关需约51%理论有效射击时间（已计护甲与属性伤害）（假设有效溅射45%），漏经济路线64%/58%；这是可行性上界，不含稀有强化/地图支援，绝非实际通关。没有按脚本失败加保底。完整人工30分钟流程仍未验证。

文本证据：[奖励/触屏/离线](../reports/qa/v13-squad-rewards.json)、[指令](../reports/qa/v13-commands.json)、[边缘/坡脚](../reports/qa/v13-navigation.json)、[满编](../reports/qa/v13-crowding.json)、[负载](../reports/qa/v13-load.json)、[状态](../reports/STATUS.json)。截图在本地`reports/local/qa-v13-*`。

实际命令：`npm test`、`npm run typecheck`、`npm run build`、`node --import tsx tools/budget-envelope.mts`、`node --import tsx tools/audit-map-crowding.mts`、`node --import tsx tools/audit-map-navigation.mts`（`SC2_MAP_AUDIT_SHOULDERS=1`）、`node tools/qa-squad-load.mjs`、`node tools/qa-squad-rewards.mjs`、`node tools/qa-command-engagement.mjs`（两浏览器脚本用`SC2_QA_OFFLINE=1`；PowerShell以`$env:`赋值）。

## 历史 V12：小地图、高低地和地图审计（2026-09-22）

运行代码 `2f22af9`。**253/253 单元／规则测试通过，类型检查、构建通过；满编拥堵诊断尚未全部通过。**

- 新增战术小地图，实际鼠标右键移动和左右键敌方集火接敌有效；390×844与844×390触屏轻点通过，与摇杆／技能／兵种栏无覆盖，0文字选中／滚动。暂停隐藏小地图，不透传命令。没有手机或实物手柄测试。
- 原 Kairos 高低地：枪兵在2单位高差崖边，两秒对低地蟑螂造成12实际伤害；隔崖近战仍禁用，独立更高山脊仍阻挡。
- 修复三关坦克边缘路线卡点；288条单兵边缘路线、288次坡脚直行／斜向／双向测试通过，0越界。真实World四兵种往返1/3/6/9/12关最远点共10段通过。
- **满编20人停靠坡口：32场景中7个90秒后仍有坦克明显偏离自身目标（>3单位），未通过。** 3号反向、4号双向、8号反向、9号反向、13号双向。正常队尾停在坡下不算失败。这个诊断明确与单兵／四人结果分开，不声称全体量寻路正常。
- 地表8层独立重复/mip，保留原像素；移除整块陡角删面造成的坡脚洞。51,072三角形、25,857顶点，无非有限坐标或WebGL错误；16坡道截图在本地。没有人工像素一致性批准，不能承诺画面无任何异常。
- 地图保持1:1。单枪兵到首关最远测试边缘8.10秒，末关52.48秒；无交战四兵种末关去程46.68秒。实战交火／手动架炮可能更慢，没有因地图大自动加速或减少敌人。
- 最终单HTML **176,519,356 bytes / 168.34 MiB**，581内嵌素材，SHA-256 `83dfbd65fcf22e0e8be36e9456cdd9eee9322079b48ffa3cfb3eeaefee23d734`。断网file://启动和小地图下令通过，HTTP(S)请求0；8战斗模型、292地图模型载入。模型/图标/声音本轮不做新的人工外观或听感宣称。

命令：`npm test`、`npm run typecheck`、`npm run build`；`node --import tsx tools/audit-map-navigation.mts`（边缘+直行，坡脚侧向用SC2_MAP_AUDIT_RAMPS_ONLY=1及SC2_MAP_AUDIT_SHOULDERS=1）；`node --import tsx tools/audit-map-squad.mts`；`node --import tsx tools/audit-map-crowding.mts`（已知失败）；`SC2_QA_OFFLINE=1 node tools/qa-map.mjs`。PowerShell环境变量用 `$env:` 赋值。

文本证据：[浏览器与离线](../reports/qa/v12-map.json)、[通行](../reports/qa/v12-navigation.json)、[混编与高低地](../reports/qa/v12-squad.json)、[满编堵点](../reports/qa/v12-crowding.json)。截图仅 `reports/local/qa-v12-map/`。旧性能数字不代表新增小地图/材质后的测量。

## 前一轮 V11：左键自动寻路集火（2026-09-22）

运行代码 `e5b472a`。删除误加的左键站定模式，左右键点敌共用寻路接敌／优先集火，目标移动继续追踪，死亡后恢复自动索敌。界面与规则同步更正。

- `npm test` **246/246 通过**；`npm run typecheck`、`npm run build` 通过。替换站定断言，覆盖主动接敌、移动目标、绕崖经坡道进入射程；保留移动射击、连续指令周期、手动架炮、治疗和手柄接管回归。
- 实际桌面左键点击远处敌人：锚点和单位均移动超过2单位后造成伤害；目标再次移开后继续追踪开火；指定目标死亡后重新攻击附近敌人。右键、HUD、连续点击、手柄转鼠标首击、390×844／844×390触控仿真继续通过。没有实物手柄或手机真机测试。
- 断网file://启动及操作通过，HTTP(S)请求0；最终HTML **176,510,352 bytes / 168.33 MiB**，SHA-256 `33cb281aca6794e987812728d6f69200d381a9b625ad751fdca764064abf2fa4`，581内嵌素材。
- 浏览器命令：`SC2_QA_OFFLINE=1 node tools/qa-command-engagement.mjs`（PowerShell环境变量赋值）。[文本证据](../reports/qa/v11-left-focus.json)；截图仅在 `reports/local/qa-v11-left-focus/`。下方站定行为为已被本轮取代的历史记录。

## 前一轮 V10：玩家指令与索敌（2026-09-22）

运行代码 `c0818ff`；**246/246 测试、类型检查、构建通过**。右键前进交战／追击集火，左键站定集火；没有更改单位数值、波次、经济和素材。

- 新增11项规则回归，修复前其中10项复现失败，修复后全部通过。覆盖枪兵／恶火／坦克前进时开火、未到编队槽位仍还击、连续点击不吞出弹或重置周期、左键保持站位并优先指定目标、射程外或隔崖目标下的合法本地还击、重新转回指定目标、死亡后继续索敌、坦克射程盲区、医疗艇移动命令期间治疗。保留恶火行军不为后方自动目标反复掉头与手动架炮。
- 实际桌面 Chrome 鼠标事件：右键移动途中产生伤害；模拟槽位阻塞仍开火；左键集火保持锚点和士兵位置、优先玩家目标；左键地面和HUD不覆盖集火；指定目标离开／进入射程和死亡后自动还击；连续右键不中断出弹；右键敌人追击。手柄API模拟后，第一条鼠标指令保留，未被接管清理吃掉。
- 390×844、844×390 CDP轻点地面交战／点敌追击通过，无选中文字和页面滚动。本轮没有手机真机或实物手柄测试。完整多点触摸历史证据仍见V9，不冒充本轮新增验收。
- 最终HTML **176,510,665 bytes / 168.33 MiB**，581内嵌素材。断网file://启动、右键指令和键盘接管通过，HTTP(S)请求0，8/8战斗模型加载、无修改状态Debug接口。SHA-256 `88eec89616515ec29c3031ed5280b49f36d3718d989d7efbdea70dcb27cb5795`。
- 本轮只验证操作，不运行自动通关，不调整普通难度。没有重测性能、完整人工流程或原客户端视觉；下方V9负载与首关观察均为历史版本。

命令：`node --import tsx --test test/command-engagement.test.ts test/commands.test.ts test/gamepad.test.ts`、`npm test`、`npm run typecheck`、`npm run build`、`SC2_QA_OFFLINE=1 node tools/qa-command-engagement.mjs`（PowerShell用环境变量赋值）。

[公开文本证据](../reports/qa/v10-engagement.json)；日志和截图仅保存在 `reports/local/qa-v10-engagement/`、`reports/local/tests-v10.log`、`reports/local/build-v10.log`，不作为人工视觉批准。

## 前一轮 V9：原地图、资源、多买与手柄

运行代码 `0e43fad`，分支 `v3/playable-sc2-demo`，继续更新 PR #2。**235/235 测试、类型检查、构建通过**。以下旧版记录保留历史版本标签，不作为新版实测。

- 真实 Kairos Junction LE，12个递增连通范围，原比例1:1。292地图模型、4,956模型摆放、2,473崖壁、179共享图片；无缺失运行素材。511源文件哈希锁定，本地完整管线重跑通过。四兵种跨原坡道行军120模拟秒诊断，最后编队槽误差约0.08单位；不评价普通难度通关。
- 原 Crystal / GasCanister / SpaceMineralCluster 3/3；战斗模型8/8、原动作8套、死亡8套、经济模型3/3、原仓体、图标23/23解码、原声音10/10解码、效果贴图33张。**这些是来源/加载/播放与数值证据，不是人工视觉批准。** 原图中立装饰不再误套敌方队伍色，并使用原摆放色调。
- 标准手柄模拟：部署、自动选敌、扳机换目标、松杆追击、暂停、多买、售罄禁用、刷新后焦点、两轮90矿共享费用、B聚焦继续、断连清输入并暂停、回中立后明确恢复。键盘接管后，即使旧手柄仍压着摇杆也不会抢回输入。非标准手柄模拟：自定义面键/摇杆/轴式扳机校准、本地保存、重载后使用。没有真实手柄硬件测量。
- 实际鼠标右键移动/集火、伤害与目标一致、键盘接管、HUD不透传、暂停冻结、高差拾取通过。地图扩展后检查97组崖壁实例，过期拾取范围0；更新实例时清除旧包围缓存。390×844与844×390的CDP触摸：轻点、拖动/长按/双指防误发命令、摇杆+架炮、取消/释放、两轮继续、无选中文字/页面滚动均通过；它们不是手机真机测试。
- 最终离线HTML断网file://载入并操作，HTTP(S)请求0，8原战斗模型、3资源模型和292地图模型载入，开发修改接口不存在。**176,509,513 bytes / 168.33 MiB**，581内嵌资源；SHA-256见STATUS。

桌面独立负载：Chrome、AMD Radeon集显/ANGLE D3D11、1440×900、DPR1、均衡画质、无CPU节流，25友军/300敌军诊断。最终运行代码预热5秒后采样464帧，平均 **38.61 FPS**，P95 **33.5 ms**，**317 draw calls**，结束模拟积压 **0.2334秒**。诊断使用高HP、补充敌人、关闭胆汁；不是自然波次或通关结果。较早独立样本45.31 FPS保留在Git历史，本次交付采用最终代码的最新样本；不把有并发测试干扰的样本计入结论。证据见v9-load。

真实时间首关观察：旧键盘策略约62.38秒失败，击杀4、救SCV1、失败仓1、无人存活；未修改HP/钱包/出生或时间。该旧策略没有完成首关，不能把它写成成功验收，也不据此弱化普通难度。关间结算、多买/刷新/跳过通过独立场景验证。完整人工48分钟流程、原客户端逐像素/逐格对照、真手柄和本轮手机真机仍未验证。

文本证据：[操作/离线](../reports/qa/v9-controls.json)、[标准手柄](../reports/qa/v9-gamepad.json)、[校准](../reports/qa/v9-gamepad-calibration.json)、[原图与寻路](../reports/qa/v9-map-routes.json)、[首关观察](../reports/qa/v9-opening.json)、[独立负载](../reports/qa/v9-load.json)、[完整状态](../reports/STATUS.json)。截图仅在 `reports/local/qa-v9*`。原客户端粒子、动态材质、风动、部分占地代理与字体边界见 [MAP_PIPELINE.md](MAP_PIPELINE.md)。

实际命令：`npm run assets:map`、`npm test`、`npm run typecheck`、`npm run build`、`node --import tsx tools/qa-map-routes.mjs`、`node tools/qa-gamepad-shop.mjs`、`node tools/qa-gamepad-calibration.mjs`、`node tools/qa-v9-opening.mjs`、`node tools/qa-v9-load.mjs`、`node tools/qa-v9-controls.mjs`（含 `SC2_QA_OFFLINE=1`）。


2026-09-21 · 运行代码 `30e173ad9346a111ac9dfb5e752c1d06a62dced4` · `D:\星际` · `v3/playable-sc2-demo` · [PR #2](https://github.com/ziyuuu/sc2-survivors-/pull/2)。当前数值、旧版真机与人工视觉结论分开记录。

## 点选操作验收

- 电脑实际鼠标右键地面移动、右键敌人集火通过；左键和 HUD 不发指令，右键菜单被阻止。键盘方向立即接管。暂停保持命令且模拟时间冻结，目标死亡解除集火。实际高台拾取使用高度 3 的落点，不投影到下方平地。
- 浏览器 390×844、844×390 的 CDP 触摸事件：轻点移动／集火通过，拖动、长按、取消、双指战场触摸不下令；摇杆＋架炮按钮同时有效，松手后方向归零，0 文字选中、0 页面滚动，两轮跳过仍可操作。这是桌面触屏仿真，本次没有荣耀真机测试。
- 新增 17 个规则／拾取测试：真实跟随与到位自动战斗、混编射程、Medivac 合法治疗、部署坦克等待手动收炮、替换目标保持冷却、重复集火不打断出弹、新救援兵继承移动、坡道／障碍路由、触控容差不改实体半径、垂直崖壁不穿透点选。
- 断网单文件再次实际右键移动、键盘接管通过，0 网络请求、0 浏览器／渲染错误。绿色移动圈与红色目标圈已生成本地截图；没有以截图保存宣称人工视觉批准。
- 当前回归仅验证输入与规则，不进行自动策略通关，不修改难度或经济预算。此前自然首关记录、压力与真机样本保持历史标签。
- 原始记录：`reports/local/qa-v8-commands/REPORT.json`；可公开文本：[v8-commands.json](../reports/qa/v8-commands.json)。执行：`node tools/qa-point-commands.mjs`，离线复验时设置环境变量 `SC2_QA_OFFLINE=1`。

## 前一轮试玩反馈的处理

- 前两关首批在 5–7 秒生成，约 9–10 秒一批。第一关 12 次单只跳虫，第二关普通 13 次双只；简单第二关 19 只分到 13 次。两关仍为 120 秒；每仓守军、单只 HP、每次掉落和通关奖励不变，第三关以后波量不变。
- 已升级重工厂筹备有容量的坦克。下一个订单成本保留在可见钱包中，其他建筑只用真实余量，付款后轮转；关间卡牌可以使用该钱包，未开工不扣款。星港与坦克实验室没有先后依赖。
- 车辆按制动距离刹停，有限转向／实体碰撞保留；坦克炮塔冷却中提前瞄准，车辆不再停在编队槽位却够不到近处敌人。架炮不移动，收炮后寻路针对当前指向点。
- 原 Char 纹理增加高台／平地差异、崖脚阴影、坡道铺面和岩石连续底床。坡道和障碍位置读取共享地图，未改变可通行性。

## 测试、构建与离线

- `npm test`：**221 通过，0 失败**；`npm run typecheck` 和 `npm run build` 退出 0。Vite 仍提示大 JS 块。
- 单文件 **80,873,870 bytes / 77.13 MiB**，102 个内嵌资产；SHA-256 `922011ecbf6d63a7b89e5afea9f6f1e0bf68adc86beb523750f820eb90c9461e`。
- 断网 `file://` 启动：0 HTTP(S) 请求；8 个战斗模型、原动作及 8 个死亡模型、原仓体、3 个经济模型载入；10 个原声音全部解码。朋友版没有修改状态的 Debug API。
- 新回归覆盖低速靠点／反向转弯／部署坦克绕行／过期指令、炮塔冷却中瞄准、射程外接敌、高级单位资源逐步到账不会被低级兵耗尽、跨建筑轮转、容量释放、暂停与钱包消费、两关小波次和最大空档。

## 历史桌面记录（bf486d8）

Chrome 1440×900 原生 DPR。正常时间和真实键盘输入，未改 HP、钱、兵力、出生或时钟；策略脚本只选择移动方向。该观察不作为普通难度门槛。

- 首关 120 秒进入关间，普通波次生成 **12** 只，成功救援 **3** 个仓，救出 **2** 个 SCV，击杀 **2** 个 Drone。
- +80 矿／+30 气通关奖励显示并到账，建筑轮／随机轮分别跳过后进入第二关。
- 独立诊断覆盖升级卡实际付款 50/25、星港／实验室独立解锁、T 架起后移动锚点不带走坦克、收起后回归当前目标、SCV 语音一次播放、12 个射击声占用时仍播放救援语音、治疗短断不重启循环和暂停立即停声。
- 23/23 原图标实际浏览器解码。东西坡道、北部坡道和岩石通道均生成本地截图，着色器错误 0；颜色／高度属性与共享地图核对通过。没有宣称人工对照 SC2 视觉通过。

3 米直行到点并停稳，恶火 **2.28→0.73 秒**、坦克 **3.02→1.05 秒**；背向起步分别 **3.20→1.17**、**4.15→1.67 秒**。这是隔离控制对照，基础移速／武器数值未增加。完整条件见 [手感测量](../reports/qa/v7-handling.json)。

## 素材与动画

- Manifest **102/102** 可用，8/8 战斗模型与主动画、8/8 原死亡模型、3 个经济模型、仓体，23 个原图标、33 张效果纹理、8 张地表／崖壁纹理。
- 本次补齐 **10 个原声音 + 2 个武器效果源文件**。SCV 是原简体中文 Ogg；9 个原 WAV 为 IMA ADPCM，在本地转 PCM16，同采样率／声道且逐样本读回相等；原文件保留。浏览器实际播放事件与解码通过，不是人工听感批准。
- 枪兵使用取得的 Fireball_1Hot 枪口层，恶火使用自身 HellionBeam / Flame2；伤害仍只来自模拟。粒子摆放和时序是网页适配，不宣称完整复刻 SC2 粒子引擎。
- 浏览器回放八套移动、七类有武器单位的攻击事件、八套死亡、坦克两形态与变形、医疗 Stand Work 和连续 HP／能量变化；错误 0。原模型独立 M3Studio 核对 24 模型／995 骨骼／208 主片段的旧审计保持有效；本次没有修改其骨架。
- 准确来源、散列和落盘位置见 [原素材清单](ASSET_DOWNLOAD_REQUIRED.md) 与 [文本证据](../reports/qa/v6-original-sources.json)。原包、转换产物与截图均未提交公开仓库。

## 性能与手机测试范围

按最新要求，**前一轮节奏／制动／筹备预算／地形变更未追加真机或手机尺寸测试**。此前的 390×844／844×390 和荣耀横竖屏记录是历史基线，不能冒充当前版本验收。

前一轮桌面负载（bf486d8，本次未重新采样）：Chrome / ANGLE (AMD, AMD Radeon(TM) Graphics (0x00001638) Direct3D11 vs_5_0 ps_5_0, D3D11)，1440×900，DPR 1，均衡，无 CPU 降速。25 友军、约 300 敌军的持续补充诊断，关闭胆汁并提高 HP，预热 5 秒后采样约 12 秒。

| 平均 FPS | P95 ms | Draw calls | 采样帧 | 模拟推进秒 | 末尾积压秒 |
|---:|---:|---:|---:|---:|---:|
| 57.1 | 33.0 | 36 | 685 | 11.65 | 3.08 |

压力场景仍有模拟积压；保留固定 tick、不丢规则时间，不把渲染 FPS 当实时推进证明。诊断与自然关卡、手机性能无等价关系。

历史真机：HONOR AMG-AN00 / Android 16 / SM8650 / Adreno 750 / Chrome 148，均衡 DPR 1.5，每场预热 5 秒后完整 60 秒采样。开局竖／横 59.9 / 59.8 FPS；25/300 诊断 58.0 / 55.5 FPS，末尾积压 2.64 / 5.11 秒。10 个声音解码及两指输入通过；这是最新反馈前的快照，无人工听感或长期温升结论。[历史真机摘要](../reports/qa/v6-phone-summary.json)。

## 边界与复现

原字体、完整粒子／折射／动态材质、人工视觉批准和完整人工 48 分钟流程仍未完成。原门骨骼开舱及已标注死亡变体保持网页适配。请求补充的 10 个声音和两个效果源文件已不再缺失。

实际命令：`npm test`、`npm run typecheck`、`npm run build`、`npm run assets:originals`（另执行 `-Refetch` 与本地转换）、`node tools/import-m3-pack.mjs fx.muzzle fx.flame`、`node tools/qa-fixes-v6.mjs`（本轮设置 `SC2_QA_DESKTOP_ONLY=1`）、`node tools/qa-pace-desktop.mjs`、`node tools/qa-effects.mjs`、`node tools/qa-navigation-load.mjs`、`node --import tsx tools/handling-profile.mts`、`node --import tsx tools/budget-envelope.mts`。本次新增点选操作另执行 `node tools/qa-point-commands.mjs`（含 `SC2_QA_OFFLINE=1`）；只做触屏仿真，不执行真机脚本。

公开摘要：[v7-summary.json](../reports/qa/v7-summary.json)、[STATUS.json](../reports/STATUS.json)。原始图像和日志仅在 `reports/local/qa-v7-desktop/`、`qa-v7-fixes/`、`qa-v6-assets/` 与 `qa-v6-load/`；历史失败／中断日志不计入通过样本。条件经济账本见 [BALANCE_12_STAGES.md](BALANCE_12_STAGES.md)，不是自动通关证据。
