# M5 内容进度与发布前缺口（2026-09-25）

用户要求在 1.0 MVP 完成后统一验收画面。本记录是工程进度，不代替 M4-02、M5-03 或 M7 的视觉与实战签收；批准的 M0 r6 数值未改。

## 已实现与已复测

- 运行目录为三族各 30 款精英变体，共 90 款；每族六名英雄，共 18 名，其中大和战列巡洋舰、HotS 利维坦和净化者旗舰使用锁定版本的 SC 战斗主体。玩家每局仍最多登记三名英雄。
- 九基础样板扩展到完整三型：变体共享同家族模型与原动作，专属战斗效果逐项接入。三舰技能、所属截击机的付款与死亡规则、泰凯斯火焰和诺娃冰霜已进入模拟事件及存档路径；利维坦固有恢复按锁定来源的每秒 2 生命读取一次。
- 三舰肖像从锁定的 `5.0.16.97563` 来源提取并列入资源溯源清单。`npm run assets:prepare` 输出 751/751、必需缺失 0；M5 浏览器 QA 逐一加载 90 个精英身份和 18 个英雄身份，均有原动画片段，18 个英雄肖像可用，六组场景无页面错误，最大 draw calls 152。该检查验证文件、解码、动画绑定和运行渲染，不等同逐款人工美术签收。
- `npm run typecheck`、`npm test`（451/451）、`npm run docs:check` 通过；独立离线 HTML 的新局、保存、重读通过。M5 浏览器证据在 `reports/local/qa-m5/report.json`，规则回归见 `test/m5-elites.test.ts`、`test/m5-air-heroes.test.ts`、`test/m5-hero-fire-ice.test.ts`、`test/m5-hero-control-event.test.ts`、`test/m5-hero-target-contract.test.ts`。

## M5-05 客观核验（已完成，非画面签收）

- `tools/qa-m5-objective.mts` 逐项验证30家族各三款共90个精英ID，50个新增变体的模板与专属数值和已批准目录一致；实测 Rank 1／5 相对普通 Rank 5 的直接输出或恢复倍率。81款可直接普攻的精英确实发射、命中；其余9款为3款航母及6款纯治疗／维修单位。90款均有死亡事务、死亡表现事件及当前格式存读，18名英雄×1／3／5级的54次技能受益、事件及存读均通过。大和和利维坦的锁定源字节哈希复核通过。证据：`reports/local/qa-m5-objective/report.json`。
- `tools/qa-m5-hero-trials.mts` 用完整60Hz战斗步进，对18名英雄×三级×单目标／五目标／混合地空进行162组60秒受控记录，分别输出10／30／60秒真实伤害、有效治疗／回盾、控制目标秒数及施法次数；162组均无“有合法目标但零作用”。`tools/qa-m5-hero-target-matrix.mts` 另对15名攻击／控制英雄的地面、空中、机械重甲、Boss、静态主巢做75项合法性与命中检查，全部符合已批准的目标限制；三名支援英雄另在54项技能核验中覆盖。以上是固定夹具，不能推出战役胜率。证据：`reports/local/qa-m5-hero-trials.json`、`reports/local/qa-m5-hero-target-matrix.json`。
- 浏览器逐身份加载结果为90精英、18英雄、6组场景、无页面错误。90个精英身份均有原攻击动作映射；78个有原模型的出手节点，余下12个是爆虫／雷兽近战或医疗艇／科技球支援。18英雄均有原动作映射，17个有出手节点；沃拉尊近战无需枪口。德哈卡和菲尼克斯在原模型缺少独立Spell时用原Attack动作，净化者旗舰用原Stand舰体动作及专属弹道。已修正死神、潜伏者、不朽者的侧边、异龙的底部和女妖的原导弹舱节点，不再从默认中心发射。
- 死亡表现现况：11款在主体中有死亡片段，10款有独立原死亡模型，69款使用程序化倒下与死亡事件。后者行为正确，但模型死亡观感仍在M5-03的MVP统一视觉验收内，不能将这份机器报告当美术签收。
- 本轮修复了三处已证明的游戏错误：沃拉尊停滞生效却没有命中表现事件；净化者旗舰单独存活被误判全队无战斗力；雷诺／凯瑞甘／阿拉纳克的行进直线技能能选静态主巢却无法打中。同时拒绝沃拉尊对无法停止的建筑空耗冷却。相应回归在上述测试中。

## M5-04 构筑与自然实战（进行中）

九Build×种子7／271／89241共27组无战损经济生产模型通过，五家族各至少一人到场的关数依次为T1第7、T2第8、T3第12、Z1第7、Z2第9、Z3第11、P1第9、P2第10、P3第11关；27组均购买计划的10项发展动作，钱包守恒。模型按实际付款、训练、出舱及换组计算，但假设救援完美且没有伤亡；多数Build至17关仍未达25身体，不能称为自然成形或平衡通过。逐关钱包、生产配置、到场和换组见 `reports/local/expedition-builds-formation-first.json`。

将输入机器人上限放宽到18关后，固定种子、零天赋、普通难度、原图的三族尝试分别在人族第1关、虫族第4关、神族第4关失败；无调试送钱或锁血。该机器人不会像玩家一样规划技能、围绕守军走位或处理全部威胁，故只定位失败场景，不判断难度是否合理。结果见 `reports/local/expedition-survival-original-v5-stages18.json`。完整18关、入无尽及41／80点代表章节的自然操作复测仍待执行，M5-04与MVP完成状态保持开放。

## 尚未作为 M5 完成验收的内容

- 108 个身份的原动作、挂点、肤色差异、普攻弹道和敌方预警遮挡仍需在 MVP 总验收中逐项人工判断。程序化 QA 不能证明“出手爽感”。M4-02 的用户视觉签收按用户要求保留到 MVP 完成时。
- 九套 Build 的无战损经济可达模型，三种子中五槽成形关为第 7—12 关；其中重机械／舰队较晚。模型假设完美救援、不计战损，不可当作可自然通关。将自动输入夹具更新到当前 `mvp-1.0` 运行入口后，原图零天赋 v2 控制器的人／虫／神分别在第 3／2／2 关失败；v3 的更早撤退策略得到第 1／3／3 关失败。v4 改从实际最近战斗成员而非小队锚点选择接敌位置，分别在第 1／4／2 关失败；神族首个空投的两名剩余守军位于舱体另一侧，v2 锚点已抵舱旁而唯一近战成员落后，导致机器人在守军近旁停步。三套输入策略差异显著，不能直接归因于数值平衡，也不能证明第 18 关可玩。详见 `reports/local/expedition-builds-formation-first.json`、`reports/local/expedition-survival-original-v2.json`、`-v3.json` 和 `-v4.json`。
- 定位并修正一处真实的近战救援停位：近战成员追击空投舱另一侧的守军时，旧静止障碍处理可能把“被友军舱体占住的目标点”当作整队停下的理由。现在有可追击的近处敌人时继续尝试绕舱，仍保留无战斗目标时的停位。修正后 v5 输入夹具在同一原图、种子和零天赋条件下，虫族与神族均完成前三关并在第四关失败；神族完成三次空投救援。人族仍在第一关失败。这个夹具缺少人类战术与完整技能操作，只用于定位路径问题，不作为三族平衡验收；记录在 `reports/local/expedition-survival-original-v5.json`。另将公开移动命令限定为数值坐标，避免传入舱体对象时把实体字段写进订单和存档。两项回归见 `test/pod-melee-path.test.ts` 与 `test/squad-ai-v14.test.ts`。
- 英雄 10/30/60 秒受控夹具已经完成；九 Build 正常游玩、三族全 18 关与无尽的完整人工路线、三种输入实体设备、正式硬件 60+ 仍待 M5-04、M6、M7；在这些结果齐全前不标记 M5 或 1.0 MVP 完成。

当前本地试玩包为 `D:\星际\dist\SC2-Survivors-Demo.html`，351,911,708 字节（335.61 MiB），SHA-256 `A3B900BB79F5C34C97B78E97EF48D5867B7F86FDEA2A15CE6F3A754120BE5E30`。构建内嵌751项素材，必需缺失0；包体预算与持续帧率仍由M6独立验收，不以本次M5核验代替。

本轮实际执行：`npm run typecheck`、`npm test`（451/451）、`npm run docs:data`、`npm run docs:check`、`node tools/check-mvp-plan.mjs --write-board`、`node --import tsx tools/qa-m5-objective.mts`、`node --import tsx tools/qa-m5-hero-trials.mts`、`node --import tsx tools/qa-m5-hero-target-matrix.mts`、`node --import tsx tools/qa-expedition-builds.mts`、`node tools/qa-m5.mjs` 和 `npm run build` 均通过。`node tools/qa-m6-offline-matrix.mjs` 对最终包做人族普通／虫族困难／神族地狱三组断网新局、保存、重新打开、读档与原难度保持检查，3/3通过，见 `reports/local/qa-m6-offline-matrix/report.json`。另执行 `node --import tsx tools/qa-expedition-survival.mts --original --v5 --stages 18`，程序无异常但输入机器人按上文关卡战败。一次浏览器复测在修改源文件触发Vite热重载时中断；源文件稳定后的完整复测为6/6场景、90/90精英、18/18英雄，无页面错误。`git diff --check` 无空白错误。所有画面文件仅保存在本地 `reports/local`。

## 非人工收尾增量（2026-09-25）

针对旧输入机器人频繁重发移动命令、早期优先生产昂贵家族的问题，新增只调用公开移动／生产／关间命令的 v6/v7 诊断策略，并用原地图、普通难度、零天赋、种子7／271／89241跑三族×三策略共27次。完整矩阵见 `reports/local/m7-natural-controller-matrix.json`：最好成绩人族完成5关、虫族3关、神族3关，18关通关数0，程序异常0。v7让人族在种子89241下由首关失败推进到第6关，但同策略会使虫族／神族更早失败；这是控制策略差异，**不能推论任何种族的数值平衡或真人可通关性**。未使用锁血、送钱、清敌或直改科技。M5-04和A38保持开放。

由公开命令实际推进产生并通过当前档案校验的第2—4关三族存档，以及人族第5—6关存档，保存在 `reports/local/natural-checkpoints/`。它们只供同一规则版本的性能诊断；第4关之后的完整自然战役和九Build有战损实战仍无合格证据。
