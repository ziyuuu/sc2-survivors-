# M1 实施与验证记录

日期：2026-09-25（北京时间）。M0 r6 已批准；M1-01 至 M1-05 已完成。新局只运行三族 mvp-1.0 的18关规则并冻结零天赋；M2 的原版天赋四线不在本阶段启用。

| 子项 | 实施与证据 |
| --- | --- |
| M1-01 | 逐入口替换／保留清单、基线指纹及原有未提交工作区记录，见 [运行审计](M1_RUNTIME_AUDIT.md) |
| M1-02 | 唯一三族运行入口；外壳v2、战局schema v2、档案v3；旧训练和战斗回退已退役，见 [清理核验](M1_CLEANUP_PROOF.md) |
| M1-03 | 隔离候选验证后一次性恢复；档内种族、难度、地图、种子控制 UI、敌军和奖励；读档保持暂停 |
| M1-04 | 旧开发战局只读摘要及原文导出；冻结旧价核算永久本金，矛盾拒绝，来源校验和一次性收据；原档和两份备份不改 |
| M1-05 | 精英救援模型加载任务登记与奖励通知顺序修正；满编选择、存读和重复提交只结算一次 |

| 实际命令 | 结果 |
| --- | --- |
| npm run typecheck | 通过 |
| npm test | 379/379通过；19份过时规则断言归档，不参与新规则测试 |
| npm run test:save | 20/20通过，含三族×四难度、备份、旧本金与精英事务 |
| npm run docs:data | 通过；参考数据仍列历史天赋／12关，M1运行不启用这些规则 |
| npm run docs:check | 通过 |
| node tools/check-mvp-plan.mjs | 通过；这是策划结构校验，不替代游戏验收 |
| npm run build | 通过；745/745必需资源就绪，独立离线HTML已生成 |
| npm run test:browser:save | 开发版五条真实浏览器存读场景通过 |
| npm run test:offline:save | file:// 独立包断网启动和神族／困难续档通过 |

浏览器验证：虫族／地狱存档后把菜单改为人族／简单，恢复仍为虫族／地狱且战斗暂停；关间读档不重抽；390像素关间无页面横向溢出；旧开发档字节一致导出并明确导入9点本金；满编精英选择存读后可操作且只结算一次。报告在 reports/local/qa-m1/development.json 与 offline.json。截图已保留作证据，未把自动化截图视为人工视觉签收。

可玩产物：dist/SC2-Survivors-Demo.html，440,929,898字节（420.50 MiB），SHA-256 C7EBD771A85406BBB3819FAC9F367CE9D1A1885A3F337A7E9A8F0425F1090877。Vite JS 主包1,339.76 kB。开始前同路径为440,973,335字节，SHA-256 6855597B49EC3DCF956DCF5F8747D978CAE5FBC7A1A5EED1E6772509E8EDC17D；原有193项Git变更未重置。

范围边界：M1 不实现165个原版天赋节点、M3标题菜单改版或无尽平地图；旧奖励源码仅供历史测试引用，不进入游戏包。浏览器自动化、规则测试、素材就绪和人工视觉验收分别记录。
