# M2 实施与验证记录

日期：2026-09-25（北京时间）。策划基线为已批准的 [M0 r6 天赋设计稿](../MVP10_TALENTS.md)，SHA-256 `FEE9E34CF4BA57474F6DA3D915DDB26FD8A47E909D0AA125D3E276B52E8DBDB1`；本阶段没有改写该稿的价格、概率或三族对象。M1 零天赋档案与战局只做规范化迁移，旧开发战局仍只允许导出和核实本金导入。

| 子项 | 运行结果 |
| --- | --- |
| M2-01 | 三族共165个稳定节点定义；每族资源／士兵／军队各41级、各59资源，微操17级／64资源。类型化前置和同一效果解释器接入生产、移动、战斗与截击机；117节点源码仅供历史资源核算。 |
| M2-02 | 永久档案v4、战局schema3；九份预设蓝图共用一份实际投资账。80点等于80级，逐级按层价格付款，洗点全退；事务校验revision。本局只读 `RunConfig.frozenTalents`，读档保留难度与暂停状态。 |
| M2-03／04 | 免费卡同组三张的额外领取、付费精英特约、免费／双建／一次完工、独立紫橙击杀掉落、七身体／七普通军衔、临时兵及七票付费进阶均接入原订单、容量和收据。 |
| M2-05／07 | 微操追赶、射程、冷却、转移、部署、走射和一次主目标APM包已接入；仅玩家爆虫爆炸后保留身体与军衔并停顿300固定步，敌方维持死亡。 |
| M2-06 | 四线、九预设、报价和前置原因可在局外操作；R16开局从本族五名候选中选首名英雄；额外卡、精英特约、转移成员与停顿状态有局内入口。手柄转移用右摇杆选择成员、X切换、A确认、B取消，指针调整落点不重建成员按钮。 |

| 实际命令 | 结果 |
| --- | --- |
| `npm run typecheck` | 通过 |
| `npm test` | 411／411通过；含M2三族×四难度×0／41／80点存档往返、爆虫300步、转移失败原子性、APM直接命中、建筑／卡牌／精英交易与读档收据。测试总数含只用于历史导入的旧117节点单元测试，现行玩法不引用旧树。 |
| `npm run docs:data`、`npm run docs:check` | 通过；生成数据参考列出165个现行节点、30单位、15现有英雄、40现有精英和18关。 |
| `npm run build` | 通过；745／745资源准备完成，生成本地离线HTML；Vite主JS 1,444.37 kB，仍有大块告警。 |
| `npm run test:browser:save`、`npm run test:offline:save` | 通过；M1存读、相反菜单难度、关间、旧档导出和精英救援回归仍有效。 |
| `npm run test:browser:m2` | 通过；实际UI配点／全退／切预设、41点R16自选首名英雄、神族困难冻结读档、390像素天赋／转移界面、离线包导入带天赋存档，页面错误0。 |
| `npm run test:browser:m2:gamepad` | 通过；虚拟标准手柄右摇杆选转移成员、X切换、B取消，未启动转移且页面错误0；最终手柄预览空闲事件修正后复跑。 |
| `node tools/qa-m2-touch.mjs` | 通过；390×844触屏模拟从HUD开启转移、点选成员和取消，无横向溢出或误提交。 |

本地浏览器证据：`reports/local/qa-m1/development.json`、`reports/local/qa-m1/offline.json`、`reports/local/qa-m2/report.json`、`reports/local/qa-m2/gamepad.json`、`reports/local/qa-m2/touch.json`；截图只保存在本机对应目录。浏览器测试检查了键鼠可操作的实际流程、窄屏触屏模拟、虚拟标准手柄和离线文件，不等于实体触屏、实体手柄或人工美术签收。

普通阶段性能以本机 AMD Ryzen 7 5800U／Radeon、Chrome headless、1440×900、DPR1、普通难度测试。三族×0／41／80点分别测真实开局和第6关正式波次参考场景，共18组；开局0点采20秒、其他组10秒，第6关为受控友军高生命参考编制，**不是实际通关**。每组预热2秒后采固定步、渲染和rAF；修正一次仅存在于测试夹具的`owner`字段错误后，所有有效组都处于战斗并持续推进模拟，页面错误0。18组固定步p95不高于1.7ms、rAF间隔p95约16.8ms；交付帧率范围57.31—59.96，少数样本的最长帧达133—283ms，最多观测13名友军／4名敌军／18个实体。Chrome headless常受显示节拍限制，不能以此证明实体设备稳定60+；长帧仍须在M6定位。数据见`reports/local/m2-ordinary-performance/results.json`、`reports/local/m2-talent-performance/results.json`（其中虫族错误夹具组无效）与`reports/local/m2-talent-performance-corrected/results.json`。

可玩产物：[SC2-Survivors-Demo.html](../../dist/SC2-Survivors-Demo.html)，441,065,746字节（420.63 MiB），SHA-256 `FFAB2B6FB3B6B18391EF1C3472E569F610AE742F3C02ED18FE6414E0AF426FFE`。工作区既有未提交修改未重置；本轮没有清除旧原档或备份。

M2是可玩的规则增量，尚非1.0 MVP正式完成。M3需处理标题三入口和显式无尽整备及平地图，M4／M5需交付最终英雄与精英模型和表现，M6需压缩离线包、消除普通关卡长帧并在目标设备验收60+。完整18关的人工操作、全部165节点逐项人工手感、实体触屏／手柄和美术签收不由本阶段自动化结果替代。
