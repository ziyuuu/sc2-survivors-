# M7 工程预检（2026-09-25）

本记录是按已批准 M0 r6 方案开始的 M7 可独立工程检查，**不是候选冻结、人工视觉签收或 1.0 MVP 发布证明**。M7-01 依赖 M5-04 和 M6-05，仍保持 BLOCKED；已确认的 M6 全关卡性能缺口不能用首关短测代替。

## 已执行的候选前置检查

| 检查 | 实际结果 | 证据 |
| --- | --- | --- |
| 当前规则、数据与离线单文件 | `npm run typecheck`、`npm test`（454/454）、`npm run docs:data`、`npm run docs:check`、`npm run build`、浏览器/断网存读及三族断网矩阵通过；资源就绪修复未改战斗规则，详见 M6 记录 | [M6进度](M6_PROGRESS.md)、`reports/local/qa-m6-offline-matrix/report.json` |
| 标题、读档、无尽和失败出口 | `npm run test:browser:m3` 通过，包含困难等档内难度、独立平地、触屏/虚拟手柄返回、地图资源失败重试与 WebGL 上下文丢失 | `reports/local/qa-m3/`、`reports/local/qa-m3-input/`、`reports/local/qa-m3-failure/` |
| 天赋与微操 | `node tools/qa-m6-talent-tree.mjs` 通过四线55图标、三族独立余额和390×844布局；已修正过期的 M2 浏览器脚本，`npm run test:browser:m2`、`:gamepad`、`:touch` 均通过，包括41点神族困难冻结读档、导出档断网导入、虚拟手柄/触屏转移取消 | `reports/local/qa-m6-talent-tree/report.json`、`reports/local/qa-m2/` |
| 90精英与18英雄工程样本 | `npm run test:browser:m5` 通过6组、90精英、18英雄，页面错误0；截图保存在本地，自动检查不构成主观画面签收 | `reports/local/qa-m5/report.json` |
| Web目录 | `npm run build:web` 用同一发行闭包生成751个已核验资源文件；`npm run test:browser:m7:web` 通过本地静态服务虫族困难开局、存档重载和390×844无横向溢出，HTTP/页面错误0，无可改状态调试入口 | `dist/web/web-release.json`、`reports/local/qa-m7-web/report.json` |

Web 目录在后续读档预热修复后重建为 **755个文件、448,047,089字节**，其中已核验资源446,485,507字节。入口 `dist/web/index.html` 的 SHA-256 为 `45DB8BE43D3E0BD139E0009BCABBB2D89D785B3DB74C88D7840C98DFD57266F4`，脚本 `dist/web/assets/index-BqjK075n.js` 的 SHA-256 为 `D831BC3379B1079B478ECB75F1E7A3B984DA6D0689F3F655034CD1CDB7DCE3C1`。`npm run test:browser:m7:web` 已在新构建上复测通过。该目录是**本地预检产物**，尚未冻结。最新单 HTML `dist/SC2-Survivors-Demo.html` 为348,917,777字节、SHA-256 `AC8215286C0ED4F78D1CEA6CF8A35F2D5FF232497B69751E078EF7D32CBD6E1F`。

预检发现原 `qa-m2` 三个浏览器脚本仍等待 M3 已移除的旧菜单入口；现已改为真实三入口和资源就绪流程，并适配三族独立资源。修订时旧文案断言与 Web 存读的错误时间基准各造成一次**测试脚本自身失败**，修复后重跑通过，未发现对应游戏规则故障。所有截图仅保存在本地；本轮未对私有 SC 画面使用远程图像服务，也未把截图自动通过当作人工视觉验收。

## 仍阻止 M7 候选冻结的条件

- M5-04：九套 Build 的自然经济/完整三族战役仍未验收；当前零天赋自动输入样本不能产出合法后期检查点。
- M6-03/04/05：三族第1—6关三种子、10—12关、16—18关与正常无尽各180秒，以及0/41/80点和首用三舰的实体设备帧时/内存对照未完成。首关60秒改善见 M6 记录，但不替代这些门槛。
- M4-02、M5-03 与 M7-02：用户要求在 MVP 内容完成时统一验收模型差异、攻击爽感、预警与真实输入。本次自动截图和虚拟输入只提供准备材料。

保持当前 `mvp-1.0` 规则标识，不能把本地预检目录或本记录标为 1.0 MVP 候选冻结。待依赖完成，再在同一提交/资源指纹上重新执行 M7-01 全部发布命令和人工验收。

## 后续非人工收尾增量（2026-09-25）

本轮修复了第4关等中途读档后的资源就绪遗漏：固定波次敌军、已启用未来产出和已付款订单模型现在于加载页完成首用准备。三族第4关合法存档的可见 Chrome／离线短测及454项规则测试见 [M6进度](M6_PROGRESS.md)。当前离线包为348,917,777字节、SHA-256 `AC8215286C0ED4F78D1CEA6CF8A35F2D5FF232497B69751E078EF7D32CBD6E1F`。三族断网新局与存读3/3通过。

三族×三种合法输入策略×三种子共27次零天赋普通战役自动操作，最高分别只完成5／3／3关；策略使用可观察玩法状态但缺少玩家级战术，不可据此宣称平衡失败或已自然通关。九Build有战损记录、三族完整18关、完整第1—6关三种子、后期／无尽自然存档和180秒性能样本仍未取得。`M5-04`、`M6-03/04/05`及`M7-01`保持开放；`M7-02`的用户视觉与真实输入验收仍依用户要求留到MVP候选内容完成后。
