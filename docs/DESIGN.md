# SC2 Survivors：当前设计基准

2026-09-21：用户批准简单／普通双难度实施方案。P0 新规则已进入同一个 World；素材与完整试玩验收单独记录，不以规则测试替代。

## USER_CONFIRMED

- 开局 1 名 Rank 1 Marine、1 座已建成兵营、50 矿／0 气。
- 12 关：1–3 每关 60 秒、4–6 每关 120 秒、7–9 每关 180 秒、10–12 每关 240 秒，总计 30 分钟。最终关摧毁虫巢并存活到结算。暂停冻结所有模拟。
- 本轮简单、普通；困难与地狱留到后续，地狱前三关届时复用困难。
- 自动生产按建筑独立工作并立即支付开工成本；重工厂初始生产 Hellion，独立科技实验室完成后预留下一单 Tank 预算，Tank 培养容量已满时才回退 Hellion。一个完成订单对应一个原人族降落仓；无局内购买／建造／排队入口。
- 降落仓下落、落地遭围攻、清除守军及附近战斗威胁后开舱、士兵真实归队。没有超时；只在舱体被毁时失败、资源不退。跨关保留。
- 前两关各三个、以后每关两个 SCV 虫卵，生成开始 30 秒倒计时；打破后经济收益结算一次，亮相与语音后退场，不占战斗名额。Drone 是高收益敌方经济目标，不增加普通战斗兵种。
- 我方优先处理战斗威胁，再攻击虫卵／Drone。保留 Marine 自动短停射击，出弹后可恢复跟随。Tank 仅接受玩家 T 键／手机按钮的全队架起与收起命令，保留变形耗时。
- 四种人族、四种虫族战斗单位；每种人族最多五人，每人最高五级军衔。死亡永久，补充空位得到新身份 Rank 1，否则晋升最低军衔。
- 编队有独立移动、转向、停火、避让、路径与有限追赶；不瞬移，不同步重排成圆团。
- 击杀掉落、被动采集、SCV 加成、通关奖励组成资源经济。关卡奖励在暂停后可用于三选一。
- 通关奖励到账后依次进入建筑轮、随机轮；每轮可买多项，也可直接继续，全部收费且可随机折扣。两轮共享刷新次数，50／90／130……矿递增。建筑轮刷新折扣，随机轮刷新三张不同的增援／科技／经济卡。额外增援卡付费一次，下一关投一个额外仓。
- 不按钱包过滤抽卡、不保证折扣或好卡、不预留刷新后的消费资金、不根据伤亡偷偷减压、不跨关免费回满 HP。
- 原地形资源与人族 HUD；地图向外开放，事件在已开放的连通可走区域生成。正常 UI 只有游戏信息，诊断在开发版 F1。
- Three.js 工程版与单 HTML 离线版使用同一个模拟。艺术素材只在本地处理；不以占位单位伪装原模型；缺失素材列明。

## 首轮数值与证据

`src/data/stages.ts` 固定波次预算、时间窗、地图、每仓守军和通关收入；`src/data/economy.ts` 固定采集、工兵与掉落；SC2 单位基础值继续使用已锁定的 LotV 5.0.15 快速速度配置。

这些是首轮待实战校准数值，不能把所有种子必胜作为验收。现有 `tools/balance-profile.mjs` 与旧报告属于旧 60 秒方案的历史实验；新的完整路线必须使用当前 World，不注入假定救援成功率或固定必出卡。

## 2026-09-21 补充确认：难度验收与碰撞

- 普通难度先以合理资源、培养与兵种配置在数值上可通关为设计基准。玩家仍需实际完成救援、输出与避险；不要求低复杂度脚本通关，也不根据脚本失败加保底。
- 自动测试分为规则断言和粗略操作策略。粗略策略可以为简单难度提供参考，不能当成人类水平、普通难度门槛或站桩必胜标准。理论账本必须明示 SCV、资源拾取、救援成功及抽到卡牌的假设，不能称为实际通关。
- 增加真实碰撞体积：地面兵种、舱体和障碍物阻挡；空中层独立。不同半径影响包围、通道通过与车辆队列；拥挤修正有每步上限，不瞬移。

## 2026-09-21 下一轮修复（用户批准）

- 原模型、材质、动画及武器表现优先，修复辅助网格白块、机体缩放和降落仓动画裁剪；视觉验收不能用加载数量代替。
- 军衔 r：攻速 1+0.15(r-1)，伤害倍率 r/攻速，生命 1+0.8(r-1)，护甲增加 0.5(r-1)，最多 +2。保持最低伤害与已损失 HP；晋升不清空冷却。医疗速率、能量上限和恢复速度乘 r，不赠送能量。
- 建筑可重复，每座独立生产和扣费；首关结束只有兵营，第二关结束起开放重工厂；已有建成重工厂后可选星港，不要求坦克升级。培养容量由单位、已付费订单和未解决仓共同预占。
- 建筑轮与随机轮均可刷新：建筑轮至少一项价格改变，随机轮组合改变。跳过第一轮只进入第二轮。十二关胜利后显示奖励与战绩，不再买卡。
- Char 中央平地、东西高台、北部虫巢，两层高度 0/3；主要坡道宽 4、长 6。地面经坡道通过，空中独立高度层；地图逐关开放且不重排旧区。
- 本轮保留现有两档波次、守军、敌军属性、资源预算。只更新分析结果，不用机器通关作为普通难度门槛。

## 2026-09-21 后续修复确认

- 行进单位始终针对当前小队指向点及可走的编队位置寻路，不重放已过期的锚点轨迹。有限转向、攻击停顿、独立速度与真实碰撞保持不变。到点减速，静态障碍走缓存可见图，架炮坦克作为固定碰撞体绕行。
- 重工厂初始只能生产恶火。随机选择轮可购买该厂的科技实验室（50 矿 / 25 气，25 / 1.4 秒；同源 SC2 UnitData / FactoryAddOns）；折扣照常生效，已付款订单先完成，随后升级，不重复收费。每座重工厂独立升级，完成后按最新反馈筹备有容量的坦克。
- 星港在第二关起且已有建成重工厂时可选，不要求科技实验室。玩家自行决定先发展坦克还是医疗艇，坦克增援与攻城后勤卡要求已有完成的实验室。
- 不改波次、守军、敌人属性、资源掉落或通关奖励预算。新增实验室开销单列，不以自动通关补偿难度。

## 2026-09-21 最新试玩修正（取代此前前两关预算与即时可负担回退）

- 前两关过于空闲：首批在 5–7 秒随机出现，随后约 9–10 秒一批。第一关 12 次单只；第二关普通 13 次双只，简单按 70% 总量向上取整得到 19 只分布到 13 次。最后一批在 111–113 秒。每关仍 120 秒，守军、早期 HP、两个 Drone / 两个 SCV、单只掉落及通关奖励不变，额外收入来自实际击杀拾取。第三关以后原波次预算不变。
- 高级兵预算不能被低级订单不断花光：只保留一个跨建筑轮转的下一单筹备意图，不提前扣款。该厂选择最高已解锁且有培养容量的兵种；其他厂只有资源超过所筹备成本时才开工。筹备成本显示在 HUD；关间选择可照常花费可见钱包。开始生产仍一次性扣完整成本，完成后仅产生一个仓。
- 恶火／坦克以制动距离决定减速，短距离明确刹停；有限转向和碰撞不取消。坦克炮塔冷却中可瞄准，不等冷却结束才开始转。锚点停止后，车辆可在 leash 内补足射程外的短缺距离，进入有效射程后保持阵位，不反复掉头回槽。
- 一级单位原 HP、武器周期和伤害不变。恶火的角色优势为速度及对轻甲直线群伤；坦克为较远射程、单发伤害和玩家可选架炮。接敌/制动回归单独量化，不用普通难度自动通关替代手感判断。
- 地形保留原布局、碰撞和坡道：增加崖脚接触暗部、亮度与色温区分的高台、原岩石纹理坡面和连续岩石底床；只改善可读性，不开放隐形捷径。地形视觉仍需实际试玩反馈。
- 本次最后这些变更以桌面验证为主；用户要求暂不继续手机测试。先前真机结果作为历史基线，不冒充新版本测量。


## 2026-09-21 新增点选操作

用户要求保留原操作，同时新增电脑鼠标右键、手机轻点的寻路与集火方式。作用对象为整个小队，无需框选。

- 地面指令：锚点经当前开放区域的地形路线前往落点，士兵按自己的速度、转向、碰撞、可走编队位置跟随；行进中以移动为优先，每个士兵到位后恢复自动交战。新救出的士兵继承未完成的移动指令。
- 敌方指令：持续锁定指定的存活敌方单位、Drone、虫卵或虫巢；各有武器单位各自进入射程并停下攻击，医疗艇继续治疗。显式集火优先于自动威胁选择，目标消失后恢复自动行为。重复点同一目标不打断出弹、不重置冷却。
- WASD／方向键／摇杆方向输入可立即接管，取消未完成点选指令。暂停保留并冻结命令，关间清除命令。新的地面或敌方指令替换旧指令。
- 部署坦克原地留守、保持射程内攻击；点选不会自动收炮，玩家需 T／按钮收炮后才执行待完成的移动指令。变形耗时、各单位独立运动及硬牵引不瞬移保持不变。
- 目的地绿色圈、目标红色圈。点击依据共用地形高度与模拟实体体积，垂直崖壁阻挡点选；障碍附近落点尝试相邻可达位置。触屏适当扩大点选容差，不扩大实体碰撞体积。
- HUD、暂停和关间界面不向战场透传点击；触屏拖动、长按、取消及双指战场触摸不发轻点命令，保留摇杆与技能同时操作。
- 本轮不改变关卡、经济、兵种数值和已确认生产规则。触屏功能用浏览器 390×844／844×390 仿真回归；遵照此前要求，不追加真机测试。

## 最新批准：原地图切片、多买与手柄（覆盖前述冲突条款）

- 一张真实 1v1 母地图，锁定源文件与 SHA256，保留原始世界比例与摆放。十二关开放连通递增切片；面积比按原 width² / 112²，坡道完整开放，不清除跨关实体。先验证真实地形、遮罩、摆放与寻路，不能用手工 Char 近似图宣称完成。
- 原 Crystal / GasCanister / SpaceMineralCluster 资源掉落；混合矿气各自显示，只结算一次。
- 普通第一关 6 只单只、第二关 12 只分六批；简单第二关 9 只分六批。时间 6/26/46/66/86/106 ±1 秒。两关均四 Drone（14/38/74/98 ±2 秒）、三 SCV 虫卵（20/54/82 ±2 秒）；各卵独立 30 秒。第三关起预算不变。
- 两轮均允许多买；每个 offerId 只能购买一次，买后留在原轮；明确继续按钮，零购买也可继续。刷新 50/90/130/170…，两轮共享，下一关重置。刷新后可重新买建筑／增援／资源，经济卡正收益属于设计，不加每关次数限制、不延迟发奖。
- PC 手柄左摇杆移动；自动锁定，LT/RT 切换目标；松杆追击锁定目标。移动保留锁定、冷却与各单位独立位移；坦克不自动收炮。A 推进、X 兴奋剂、Y 架炮、B 自动选敌、Menu 暂停；菜单方向导航、A 确认、B 聚焦继续。通用手柄支持校准与本地保存。断连／失焦清输入并暂停，中立后明确继续。
- 不做真机手机测试；桌面触屏尺寸回归与模拟手柄必须和真实硬件验收分别报告。普通难度不以脚本通关为标准。

- 原图实施选择 Kairos Junction LE（2019 S2），原 Mar Sara / 工业地貌取代手工 Char 图；坐标、比例和十二个递增连通范围以 `tools/map-lock.json` 与共用MapDefinition为准。旧 CharTerrain只保留为独立回归夹具和历史实验。

## 2026-09-22 试玩修正：指令与索敌优先级

- 右键地面／手机点地为前进交战：保留指向点，行进中可短暂停步射击，出弹后继续跟随；到点或编队拥挤时仍正常索敌，不再用“抵达编队槽”作为开火条件。附近同类可攻击目标优先于射程外目标，目的地附近的敌人影响自动目标排序。
- 右键敌人／手机点敌仍为追击集火；左键敌人为站定集火，锚点及现有队员保持位置，不为射程外目标自行追走。左键地面不替换战斗指令。
- 玩家指定目标可攻击时优先于自动目标，包括刻意选择 Drone／虫卵。站定或架炮单位无法攻击指定目标时，仍可还击当前合法目标；指定目标重新可攻击时优先转回，死亡后恢复自动索敌。坦克最小射程、地形遮挡和空地合法性继续生效。
- 新移动指令不取消已承诺的出弹；切换集火可以把尚未出弹的攻击交给新的合法目标，保留有限瞄准和原攻击周期。连续点击不能制造禁火或增加射速。医疗艇在移动指令期间继续合法治疗。
- 键盘／摇杆仍可立即接管；从手柄转回鼠标时先释放旧输入，再执行本次点击，第一条鼠标指令不会被接管清理吃掉。
- 不改单位属性、关卡预算、资源和生产；本轮只修复操作、索敌与相关回归。

## 2026-09-22 操作澄清：左键集火也要寻路（覆盖站定条款）

- 左键点敌与右键点敌使用同一集火指令：目标在射程外时自主寻路靠近，进入自身武器射程后优先攻击；目标移动后继续追踪，死亡后自动索敌。删除上一轮误加的左键原地保持模式。
- 左键地面仍不发移动命令。已架炮坦克继续遵循手动收炮规则；其他可移动作战单位正常接敌，医疗艇继续治疗。移动射击、连续点击保持冷却、HUD不透传和键盘／摇杆接管规则保持有效。

## 2026-09-22 小地图、高低地与通行检查

- 战术小地图为当前开放区域的北向上等比例图：地形高度、开放边缘、相机范围、小队、敌军、救援、SCV、资源与虫巢。右键地面移动，左右键点敌寻路集火，触屏轻点移动／集火；暂停和关间不接收指令。不会直接搬动小队或越过未开放区域。
- 远程可从高台向低地射击，地形不应挡住射手自己所在的平台。当前无战争迷雾，低地向已可见高地也允许；第三处高于射击双方的山脊仍遮挡。近战必须有地面接触通路，不新增命中率或伤害加成。
- 原地图比例、开放范围、关卡和经济不变；按真实行军时间评估大小。队尾可因编队间距留在坡下，不能把队尾仍在坡下直接认定为失败；与自己合法目标长期明显相离的拥堵必须单独记录。

## 2026-09-22 新版节奏、展开与稀有支援（覆盖此前冲突条款）

- 十二关按每三关一档 60／120／180／240 秒，总战斗 30 分钟。敌人总数、守军、属性、通关奖励与地图开放范围不变，波次重新排入时长，前两关 3/13/23/33/43/53 ±0.5 秒；经济事件同比提前。跨关存活 SCV 卵保留真实 30 秒期限，关间冻结。
- 原基础移速与射程不改。八种战斗模型、原骨架挂点和碰撞半径统一乘 0.8；地图不缩。开阔地按兵种排成最多三列，目标之间保留真实半径余量；崖壁／狭路搜索彼此分开的可走位置，不把所有失败槽位退回同一锚点。单位仍独立移动、转身、刹车、碰撞，反向指令不重新摆放实体。
- 同一目标的可动火力在自身射程内向两侧展开，保留相对横向顺序；候选点需可走、可见，跨崖时先走原路径。可以在抵达展开点前开火，出弹与周期仍由模拟决定，架炮坦克原地留守。单兵已有有效射击位置时不强制绕圈。
- 每名 SCV 的增量为每秒 0.1725 矿／0.046 气，普通基础仍为 0.5／0.1。普通击杀、守军、Drone 掉落均乘 1.15；简单继续乘 1.25。不免费结算未拾取掉落。
- 已完成重工厂无需研究即可生产恶火。此前 HUD 的“恶火 · 待升级”改为“恶火 · 已解锁”；科技实验室仍只解锁坦克。建造及资源筹备仍真实耗时。
- 新科技“纳米维修系统”150 矿／75 气，拥有星港后可抽到。医疗艇按原连续治疗／能量比例修复友方机械队员，包含其他医疗艇，但不包含自身、建筑、敌人和死亡单位。它是本游戏扩展，不改写 SC2 原版治疗定义。
- 随机轮先掷稀有度：白 64%、绿 20%、蓝 10%、紫 5%、橙 1%，再抽该档合法内容；不按资源过滤。每轮三张不同支援，折扣独立，刷新与多买规则不变。建筑轮仍为普通建筑报价。
- 用户明确确认：绿卡直接获得 Rank 3，蓝卡直接获得 Rank 5，既有建筑生产仍投仓。白卡增援也仍投仓。绿／蓝队员有空位则新增，否则提升最低军衔至 3／5；不存在有益晋升时不再抽对应队员卡。升级不清冷却，保留已损失 HP，不补能量。已付费订单、有效仓及额外投放预占的容量不能挤掉。绿价格为原生产价 ×2，蓝为 ×3，再适用显示折扣。
- 紫／橙缺失的特殊单位、英雄和装备暂不伪装上线。本轮先采用已有战斗／医疗／推进能力的强化，绿至橙分别为武器 +10/20/35/60%，生命倍率 +12/25/45/75%，峰值医疗 +15/30/50/80%，推进充能 +10/20/30/45%（持续时间另增加强化值 ×0.6 秒）。这些为本游戏实验数值，武器加成同时作用于属性伤害与两种坦克模式；生命保留伤损，治疗仍耗能。
- 战斗单位每次死亡 2%、Drone 8% 概率掉免费支援；地图品质概率白84.8%、绿10%、蓝4%、紫1%、橙0.2%。使用独立原图标与颜色标识，不用缺失英雄模型冒充。拾取一次结算、暂停不吸取、不能隔崖吸取、跨关保留；已满培养等过期支援回收为其基础价格矿气并明确提示。地图白色增援拾取后投仓，绿／蓝仍直接入队。
- 正常难度不以自动操作通关为标准。保留敌人预算意味着单位时间压力提高；本轮报告理论经济和诊断通行，真实人工 30 分钟流程仍待试玩，不据脚本失利添加保底。
